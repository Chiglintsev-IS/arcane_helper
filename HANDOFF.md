# План передачи: границы контекстов к нулю долга (2026-08-03, вторая редакция)

> **Выполнен целиком 2026-08-04.** Все пятнадцать задач закрыты; что сделано — в разделах ниже, что
> дальше — в самом конце. Правила ниже остаются в силе для следующего плана.

Исполняется по одной задаче за сессию. Перед любой задачей прочитай `CLAUDE.md`,
`docs/domains/README.md` и докстринг `scripts/check-layers.py`. Правила для всех задач:

- Мелкие коммиты по одной мысли; код и спека — одним коммитом; сообщения на английском и
  осмысленные. Сообщение «wip» запрещено. Четыре уже влитых «wip» не трогать: история после
  слияния не переписывается.
- Перед завершением полный прогон, всё зелёное:
  `npm run check:docs && npm run check:layers && npm run typecheck && npm run test:coverage && npm run build`
- `npm run test:e2e` в этот список не входил, и именно там пряталась вёрстка: прогонять его каждой
  задаче, которая трогает `src/ui/**`. Сейчас зелёные все 19.
- Счётчик в выводе `check:layers` («известных долгов: N рёбер, M циклов») обязан только убывать.
  Ожидаемое значение названо в каждой задаче — если после правки оно другое, разберись до коммита.
- `--write-baseline` запрещён везде, кроме конца задачи 10: флаг молча легализует что угодно.
- Относительные импорты между контекстами запрещены — страж видит только пути `@/…`, и
  «починка» ребра относительным путём спрячет зависимость, не убрав её. Контроль:
  `grep -rn 'from "\.\./' src/core/domain` пуст.
- Имена в коде — только из `docs/glossary.md`. Механику не выдумывать.
- Если задача не даётся (прогон красный и починить не выходит) — откатить рабочие правки, ничего
  не вливать, записать причину сюда в раздел задачи и остановиться.

## Что уже влито в main

Все задачи прежней редакции плана; их недоделки стали задачами ниже и здесь не перечисляются.

- `cf65605` — ADR-0033: база КД из надетого доспеха, `miscBonuses` у персонажа, итог КД в `sheet`,
  миграция v3→v4, «Сумка» строго вещественная.
- `86a03ad` — ADR-0034: оболочка `src/ui/app/PlayShell.tsx` + шесть экранов
  `src/ui/screens/{game,book,sheet,bag,rest,journal}`; `PlayScreen.tsx` удалён, `SCREEN_PARTS`
  умер; `screenMode` ушёл из доменной схемы (экспорт v5), режим живёт в localStorage оболочки.
- `45e9260` — дефекты правильности: экономика хода только из журнала (экспорт v6), `castSpell`
  ходит через `checkAvailability`, отказ «Берётся после короткого отдыха» в сценарии отдыха,
  тариф очков переехал в `arcana`, `exchangeBlood` принимает очки; базлайн 17/18 → 15/9.
- `fe7fff6` — правила из UI: `ritualAvailable`/`isSpellReady` в core, `Equipment.idFromName`,
  единая таблица `CURRENCY_ABBREVIATIONS` в `core/shared/language.ts`, предпросмотр хитов через
  `Vitality`.
- `6bce488` — задача 1, пункт 1: шторки КД, хитов, ресурсов и концентрации принадлежат «Привалу»;
  урон на привале снова предлагает проверку концентрации; переход к полным правилам стал
  необязательным у листа концентрации, и на «Привале» его нет.
- `5bd255f` — задача 1, пункт 2: режим из localStorage сверяется со `SCREEN_MODES` с откатом в
  «Игру», обращение к хранилищу в `try/catch`, мёртвый `economy` в оболочке убран.
- `a377587` — задача 1, пункт 3: `screenMode` переехал из `core/shared` в `ui/shared/model`,
  докстринг и глоссарий говорят правду; `f037b1d` — прогон на недоступное хранилище.

- `36bf722` — корни экранов «Игры», «Книги» и «Привала» просили полную высоту оболочки, у которой
  часть высоты уже занята переключателем: документ выходил на шапку выше экрана, и прокручивалась
  вся страница. Корни стали флекс-детьми, как уже были «Сумка», «Лист» и «Журнал».
- `0b8c1f0` — задача 1а, ADR-0035: «Игра» стала одной областью прокрутки, закреплены только КД, хиты
  и ячейки; прочие значки, блок действующего, ряд кнопок и полоса фильтров уезжают со списком. Шапка
  ресурсов разделена на `ResourceHeader` (закреплённое) и `ResourceBadges` (значки). Отбор по роли
  ушёл в «Книгу». Прогон бюджета переписан: он мерит строку под закреплённой полосой, а не при
  нулевой прокрутке; прогон переключателя режима — что текущий ярлык виден, а не что влезли все
  шесть.
- `0757a8f` + `2f1d92d` — задача 2: уровни заклинаний объявляет только каталог. Аркане остался
  `MINIMUM_SPELL_LEVEL` (это про ячейку, не про заклинание), потребители переведены на
  `catalog/spell`, границы записаны в «Модель» каталога и «Границы» арканы. Долг 15/9 → 13/5.
- `4c5fcf0` — задача 3: `src/core/domain/shared/levels.ts` (границы уровня персонажа, дубль в
  каталоге умер) и `shared/schema.ts` (`nonEmpty`, `isoDateTime`, `itemBonusesSchema`,
  `ItemBonuses`, `NO_ITEM_BONUSES`). Это «основа» — раздел «Направление зависимостей» в
  `docs/domains/README.md` её называет и объясняет, почему рёбра в неё не считаются. Заодно
  `architecture.md` перестал числить режим экрана в ядре. Долг не менялся: 13/5.
- `5caf6eb` — задача 4: `vitality/schema.ts` (хиты, временные хиты, Кости, подавление), полная схема
  берёт их спредом `VITALITY_FIELDS`, `VitalityState` — собственный тип, `HitDiceCost` объявлен у
  жизнеспособности структурно вместо импорта из каталога. Умер и устаревший дубль докстринга над
  полем снаряжения. Долг 13/5 → 11/4.
- `5379bd3` — задача 5: `spellbook/schema.ts` (состав книги, заметки, пометки отыгрыша) и доводчик
  `refineSpellbook` с тремя инвариантами — сборка зовёт его в своём `superRefine`, а собственная
  схема контекста несёт его сама, и инварианты проверяются на одной книге (`schema.test.ts`).
  `SpellbookState` — собственный тип. Долг 11/4 → 10/3.
- `8649c4f` — задача 6: `equipment/schema.ts` (категории, монеты, пределы счёта и кошелька, цена,
  вещь, само поле снаряжения), тип данных назван `EquipmentData`, агрегат перешёл на свою схему.
  Словари на переходный период реэкспортируются из `character/state.ts`. Долг 10/3 → 9/2.
- `c613f26` — задача 7: `arcana/schema.ts` (ячейки, восстановление, руны, очки) и бывшее ничьё поле
  `shortRestSinceLongRest` — владелец назван строкой в таблице владения README. Агрегат его не
  правит: ставит отдых, ресурсы читают как предусловие. Долг 9/2 → 8/1.
- `15e3b51` — задача 8: `effects/schema.ts` (активные эффекты, концентрация) и доводчик
  `refineEffects` с двумя инвариантами (`schema.test.ts`). Общая схема перестала импортировать
  каталог, и вместе с `effects → character` умерло `character → catalog`. Долг 8/1 → **6/0**.
- `5ff787c` — базлайн вернулся к досессионному содержимому: он оказался переписан по ходу работы без
  флага `--write-baseline`, а его перегенерация — единственный раз в конце задачи 10. На счётчик это
  не влияло: он считается по коду, а в файл только заглядывает. Правка оказалась осознанной —
  `a9c5c8c` вернул её: в перегенерированном файле нет уже убитых записей, и страж от этого строже.
- `85cb36d` — задача 9: `JournalEntry<TState>` с состоянием параметром типа, список обратимых полей
  приходит в `Journal.of` аргументом, инстанцирует его прикладной слой. Долг 6/0 → 5/0.
- `c8ce715` + `1403f88` + `2c59d77` — задача 10: каталог `src/core/domain/assembly/` (полная схема,
  корень `Character`, приведение, ключи журнала), `character/schema.ts` со своим, `sheet/*` на
  структурных входах, `withSheet` сужен до шестнадцати полей листа, 71 файл переведён на владельцев,
  прогоны общей схемы разнесены по контекстам. Базлайн перегенерирован единственный раз: **0/0**.

Прогон зелёный целиком: 1497 тестов, покрытие 100 %, typecheck, `check:docs
--strict-link-remnants`, `check:layers` (долг 13 рёбер / 5 циклов), сборка и **все 19 e2e**.

Замеры «Игры» после переделки (320 × 568, бой начат): документ 568 — страница не прокручивается;
оболочка 73; закреплённая полоса 113 и после прокрутки остаётся на 73…186; первая строка списка
встаёт на 186…321 — целиком в экране. До переделки она кончалась на 612, а с концентрацией документ
выходил на 584.

## Границы контекстов: сделано

Долг закрыт: `scripts/layer-baseline.json` пуст, фактические рёбра совпадают с картой
`docs/domains/README.md`, она же `ALLOWED_CONTEXT_EDGES` в `scripts/check-layers.py`. Правило на
будущее: **карту не править походя** — новое ребро мимо неё теперь ошибка прогона, которой негде
спрятаться, и добавление ребра стоит отдельного коммита и записи в ADR.

Что стоит помнить, трогая схемы состояния:

- Полная схема собирается **спредом plain-объектов полей** (`*_FIELDS`) в один `z.object({...})`, а
  не `.extend()`/`.merge()`: обёртки `.default()`/`.refine()` дают не-`ZodObject`, а
  `MUTABLE_STATE_KEYS` читает `.shape` — без него журнал остался бы без ключей.
- Инварианты, видные по нескольким полям сразу, живут доводчиками у владельцев (`refineSpellbook`,
  `refineEffects`); сборка зовёт их в своём `superRefine`. Собственная схема контекста несёт доводчик
  сама — на ней его и проверяют, а прогон полной схемы следит, что сборка звать не перестала.
- Канарейка: `createThorne()` собирает Торна через `characterStateSchema.parse` и не перечисляет
  поля со значениями по умолчанию — потерянный `.default()` всплывёт именно там.
- `EXPORT_SCHEMA_VERSION` при переносе полей между модулями не поднимается: форма данных не меняется.

## Задача 11 — правила сотворения и тарифы: сделано

Шесть пунктов, пять коммитов (правило уровня ячейки закрылось вместе с согласиями — они об одном
месте). Прогон зелёный целиком: 1517 тестов, покрытие 100 %, сборка и все 19 e2e.

- `4ae05a2` — цена очков в хитах принадлежит аркане: `hitPointsForPoints`, через неё выражен
  `hitPointCost`; туда же `maximumExchangePoints` и `affordableSpellLevels`. Формулы «очки × тариф»
  в сценарии здоровья, обоих объявлениях обмена и мастере крови больше нет.
- `1930e4a` — причину «магическое восстановление сейчас не берётся» называет агрегат арканы,
  сценарий бросает её же, кнопка её же показывает. Сценарий перестал читать отметку короткого
  отдыха мимо агрегата; исчерпанный бюджет говорит «исчерпан», а не «превышает остаток 0».
- `c2facce` — предупреждение снимается тем согласием, которое объявил предикат: `enforcement` вместо
  белого списка кодов, `advisory` у тех, за которыми нет расплаты в состоянии. Согласий два, и они
  разведены и в заявке, и в черновике мастера. Дубль правила уровня ячейки в `applyPayment` умер:
  отказывает предикат.
- `1070bcc` — мёртвый код жизнеспособности убран целиком (`exchangeHitPoints`,
  `applyExchangeToHitPoints`, `regenerationApplies`, `Exchange.remainderIgnored`, `sunSaveDc`);
  FR-182 назвал живые прогоны.
- `38a403f` — FR-233 описывает все приведения, а не только v1/v2, включая «режим экрана и учёт хода
  читаются и отбрасываются»; прогон на это есть, мёртвые поля ушли из фикстур состояния.

Что стоит помнить: поле `screenMode` и учёт хода отбрасываются не приведением, а самой схемой —
`z.object` срезает незнакомые ключи. Отдельного шага приведения у них нет и не нужно.

## Задача 12 — типизированный протокол и тесты за экранами: сделано

- `da5b80f` — протокол шторок «Листа» и «Сумки» стал типом: блок листа отдаёт экрану `SheetEdit`,
  вещь названа полем, а не приставкой в строке. Девять булевых флагов «Игры» оставлены как есть:
  они локальны, через слои не текут, и один union на девять независимых шторок означал бы «не более
  одной открытой», а это решение про поведение, а не про типы.
- `fe78c95` — `src/ui/screens/play/` удалён. 2036 строк прогонов разнесены: «Игра» (два файла),
  «Книга», «Журнал», «Лист», «Сумка», «Привал» проверяются без оболочки, а оболочке досталось то,
  что существует только между экранами: переключение режима, полоса ошибки и пути «сделал здесь —
  видно там». Три проверки переехали к настоящему владельцу, а не к тому, кого рендерили.
- `f445bd8` — виджеты `Bag`, `Camp`, `Journal`, `CharacterSheet` перестали называться экранами;
  правило имени записано строкой в глоссарии.

## Задача 13 — протечки правил из UI: сделано

Тринадцать связок закрыты пятью коммитами; покрытие снова 100 %, все 19 e2e зелёные.

- `cb979f8` — `exchangeWarnings` уехала к `checkAvailability` вместе со своими прогонами.
- `7eb32da` — подробная карточка берёт способ у `bestCastPlan`: в бою она больше не объявляет
  ритуал, которого мастер применения уже не предложит.
- `9cfcca9` — диапазон возможного на костях у жизнеспособности, причина «руну не приложить» у
  арканы (FR-157 — новое требование на правило, которого в спеке не было).
- `35a4eed` — «фокусировка не заменяет» и «есть ли смысл повышать» отвечает каталог; краткая
  карточка спрашивает `isSpellReady` вместо перебора списка подготовленных.
- `d6e6623` — остальные девять: лимит подготовки сценарий берёт сам, лестница истощения и границы
  характеристики объявлены владельцем, вердикты d20 — у концентрации, отбор возвращаемых ячеек — у
  ячеек, сумма снижений — геттер жизнеспособности, состав и цена строки списка — у способов
  сотворения, предпросмотр смены уровня — рядом со сменой, литерал «зм» — из таблицы валют.

Что осталось на будущее, не в этой задаче: `castOptions.test.ts` (ядро) импортирует `actionTraits`
и `filters` из `ui/` — прогон ядра зависит от интерфейса. Страж границ смотрит только на рабочий
код, поэтому молчит.

## Задача 14 — хвосты документации: сделано

- `f652427` — все 187 названных проверок существуют в коде: девять строк «Проверка» указывали на
  умершие имена, семь e2e не принадлежали ни одному требованию, у FR-114 было оборвано «, E2E».
  Требование FR-174 («потеря хитов обменом уроном не считается») прогона не имело вовсе — написан.
- `570f4dd` — текст догнал шесть экранов: «Главный экран» стал оболочкой и «Игрой», кнопка
  «Реакции» перестала стоять в шапке, долгий отдых начинается с открытия «Привала», глоссарий
  получил `shortRestSinceLongRest`, `CURRENCY_ABBREVIATIONS` и `Equipment.idFromName`, а `roadmap`
  — новое «сегодня» и ссылку на этот план.
- `b7a5182` — четыре отложенных решения приняты: вклад карточки в КД не бывает отрицательным
  (инвариант каталога и отказ схемы), классификация эффекта `buff/control/utility/summon` убрана из
  состояния — ни одно правило её не читало, расход вещи из «Игры» стал FR-244 со статусом
  «Отложено», подпись кнопки часа назвала регенерацию. Заведён `docs/domains/sheet.md`; FR-224 и
  FR-225 переехали в него из документа персонажа.

## Задача 15 — уборка: сделано

Одиннадцать worktree-каталогов и их ветки удалены, `.claude/worktrees/` пуст. В
`agent-a0702bc…` незакоммиченные правки сверены с main перед удалением: тот же код ADR-0033, только
против прежней раскладки модулей (`character/state` вместо `assembly/state`) — потерять было нечего.
Пять отчётов агентов удалены вместе с каталогами: их находки разнесены задачами 11 и 14.

Осталась одна ветка вне плана — `agent/character-sheet` (`83f0709`, влита в main). Плана она не
касалась, поэтому не тронута: удалить её можно одной командой, когда станет мешать.

## Что дальше

План выполнен целиком. Следующий шаг — игровая сессия: приложение ставится на домашний экран,
работает в авиарежиме и знает все заклинания книги. Из незакрытого остались вопросы к мастеру,
перечисленные в «Следующем шаге» [docs/roadmap.md](docs/roadmap.md), и одна находка, на которую в
плане строки не было: прогон ядра `castOptions.test.ts` импортирует `actionTraits` и `filters` из
`ui/` — страж границ смотрит только на рабочий код и потому молчит.
