/**
 * Сколько длится короткий отдых.
 *
 * Число живёт у жизнеспособности, а не в сценарии отдыха: длительность — правило игры, и называют
 * её оба — запрет во время боя и подпись самой операции. Пока своего числа у отдыха не было, он
 * был неотличим от часа и делал всё, что делает час.
 */

import { withPlural } from "@/shared/language";

// Правило этого стола: базовые правила 2014 требуют «не короче часа».
const SHORT_REST_MINUTES = 10;

/** Длительность короткого отдыха словами: её произносят и отказ, и подпись операции. */
export const SHORT_REST_DURATION_RU = withPlural(SHORT_REST_MINUTES, [
  "минута",
  "минуты",
  "минут",
]);
