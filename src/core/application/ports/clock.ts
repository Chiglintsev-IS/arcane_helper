/**
 * Порт часов: время и идентификаторы приходят снаружи.
 *
 * Чистые функции их не изобретают — иначе один и тот же вызов дважды дал бы разное, и ни снимок
 * прогона, ни отмена не были бы воспроизводимы. Реализацию подставляет сборка: системные часы в
 * приложении, считалку — в прогоне.
 */

export type Clock = {
  now: () => string;
  nextId: () => string;
};
