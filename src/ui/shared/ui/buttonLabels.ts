/**
 * Слова кнопок, которыми набранное вступает в силу или уходит ни с чем.
 *
 * Их четыре на всё отображение, и живут они здесь, а не в компонентах: слово, набранное на месте,
 * не сверяется ни с чем, и каждый экран называет фиксацию своим глаголом. За столом на выбор между
 * глаголами уходит секунда, которой нет.
 *
 * Уход и возврат разведены нарочно: закрыть шторку и вернуть сделанное — разные дела, и общего
 * корня им не полагается.
 */

export const BUTTON_LABELS = {
  /** Правится запись листа: повтор нажатия оставит её той же. */
  save: "Сохранить",
  /** Совершается событие за столом: сотворение, отдых, возврат ячеек, урон и лечение. */
  confirm: "Подтвердить",
  /** Уход, ничего не изменивший. */
  dismiss: "Отмена",
  /** Возврат сделанного: журнал возвращает записанное им же. */
  undo: "Вернуть",
} as const;
