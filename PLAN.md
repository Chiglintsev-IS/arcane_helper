# План: шов между ядром и отображением

Рабочий документ на время переезда. Он называет пути и имена файлов, поэтому лежит в корне, а не в
`docs/`: документы спецификации абстрактны по правилу «Как писать документы». Спека этой работы —
запись в `docs/decisions.md` и раздел в `docs/architecture.md`, они пишутся отдельно и без путей.
Файл удаляется, когда последний этап закрыт.

## Зачем

Сегодня `core` и `ui` разделены по каталогам, но не по границе. Отображение зовёт ядро вызовом
функции в своём процессе, передаёт в него замыкания и исполняет домен у себя. Вынести ядро на сервер
нельзя ни одним переключением — переезд означал бы переписывание отображения целиком.

Цель — сделать шов настоящим: ядро говорит с миром сообщениями, отображение не знает о ядре ничего,
кроме договора. Оффлайн при этом остаётся основным режимом, а не деградацией: продукт — оффлайн-PWA,
и локальный провод не заглушка, а способ доставки по умолчанию.

## Признак готовности

Проверяемый, а не на глаз. Правило в `scripts/check-layers.py`:

> `src/ui/**` импортирует из `src/` только `@/contract/**`. Исключения перечислены в проверке
> поимённо; их три — композиционный корень, модуль языка и прогонная сборка сторов.

**Правило включено и держится.** Ни одного рабочего файла отображения, берущего логику, не осталось.

Второй признак ждёт Э5: `NEXT_PUBLIC_ARCANE_BACKEND=http npm run build` даёт бандл, в котором нет
`core/`. Сегодня композиционный корень тянет ядро статическим импортом — офлайн-сборка так и
поставляется, а выбросить `core/` из сетевого бандла может только динамический выбор провода.

## Структура

```
src/contract/                  договор двух сторон. Импортирует только zod.
  commands.ts                  union команд + схемы + commandId
  views.ts                     проекции + схемы
  snapshot.ts                  снимок: версия, проекции, журнал
  result.ts                    успех либо отказ с причиной
  questions.ts                 union вопросов + предпросмотры
  port.ts                      ArcaneApi
  transport.ts                 Transport
  client.ts                    createClient(transport): ArcaneApi
  httpTransport.ts             createHttpTransport(baseUrl): Transport

src/core/
  domain/                      правила                                    без изменений
  application/                 сценарии в своём словаре; про contract не знает
    useCases/                                                             без изменений
    ports/                     ведомые порты                              без изменений
    session.ts                                                            без изменений
  presentation/                ВХОДЯЩЕЕ: как в модуль обращаются
    controller.ts              команда договора → вызов сценария
    presenter.ts               состояние домена → проекции договора
    handler.ts                 unknown → разбор схемой → controller → unknown
    previewer.ts               вопрос договора → предпросмотр
    views/choicesView.ts       перечни выбора и границы набора
    localTransport.ts          провод в процессе, через JSON
  infrastructure/              ИСХОДЯЩЕЕ: чем модуль пользуется сам
    persistence/                                                          без изменений
    catalog/                                                              без изменений
    clock.ts                   системные часы — переезжают из отображения
  composition.ts               сборка четырёх слоёв

src/app/api/arcane/session/route.ts    три строки
src/app/api/arcane/command/route.ts    три строки
src/app/api/arcane/question/route.ts   три строки

src/shared/language.ts         морфология и слова правил: их произносят обе стороны

src/ui/                        только @/contract
  app/providers/stores.tsx     композиционный корень: выбор провода
```

Разделение `presentation` и `infrastructure` — по направлению, а не по технологии. В `presentation`
то, через что в ядро обращаются извне; в `infrastructure` то, к чему ядро обращается само. Класть
ведущий и ведомый адаптер в один каталог значит потерять единственное различие, ради которого
адаптеры и заведены.

## Правило зависимостей

```
contract/           → ничего из src/
shared/             → ничего из src/            ← язык; его читают обе стороны
core/domain/        → domain
core/application/   → domain, application            ← про contract НЕ знает
core/presentation/  → contract, application, domain
core/infrastructure/→ application (порты), domain
core/composition.ts → всё внутри core, contract
ui/                 → contract, shared/language
```

Ключевое здесь — что `application` не знает `contract`. Сценарии остаются в своём словаре: принимают
`Spell` и `Session`, а не `spellId` и JSON. Перевод во внешний язык и обратно делает `presentation` —
и там же однажды поглотится вторая версия провода, если она понадобится, не задев ни одного сценария.

## Решения

Долговечные решения живут в журнале решений, а не здесь: сообщения через границу — ADR-0039,
договор отдельным корнем и провода — ADR-0040, направления адаптеров и композиционный корень —
ADR-0041, идемпотентность по журналу — ADR-0042, отказ от event sourcing — ADR-0043, вопрос про
ненабранное — ADR-0044. Подходы и их зоны — architecture.md, раздел «Подходы». Здесь остаётся то,
чему в документах не место, — рабочие следствия для конкретных файлов:

- `apply(session => session)` в `sessionStore.ts` заменяется на `execute(command)`; возвращаемое
  значение становится промисом, и 53 места вызова правятся под это.
- Карточка в `CastRequest` остаётся: сценарий продолжает принимать `Spell`. `spellId → Spell`
  переводит `core/presentation/`, доставая карточку из каталога сессии.
- `systemClock()` уходит из `src/ui/app/providers/stores.tsx` в `src/core/infrastructure/clock.ts`.
- `Clock` в `core/application/` переименовывается в `Occasion` и получает `commandId`; правятся
  `commit` в `session.ts`, `Journal.append` и сигнатуры сценариев. Поиск повтора — `alreadyApplied`
  по `session.journal` до вызова сценария, в контроллере.
- Выбор провода — динамическим импортом в композиционном корне, иначе сборщик не выбросит `core/`
  из сетевого бандла.
- Идентификатор попытки едет **рядом** с командой, конвертом `{ commandId, command }`, а не полем
  внутри неё. Внутри поля не выходит без приведения типа: разобранное объединение не собирается
  обратно из «команды без поля» плюс поле, а приведения запрещены проверкой. Заодно так честнее —
  идентификатор описывает попытку доставки, а не намерение.
- Слово из закрытого списка сужает владелец списка, тем же перечнем, которым пользуется сам:
  `CAST_MODES`, `CONCENTRATION_ENDS` заводятся значениями рядом со своими типами. Составное
  значение сужает функция владельца (`itemDefinitionOf`, `moneyOf`, `permanentContributionOf`) —
  наружу отдаётся сужение, а не схема, иначе схему начнут расширять на месте.
- Список справочных полей листа объявляется значением у владельца `Identity`: тем же перечнем
  отбирается справочная часть присланной правки.
- Фраза, посчитанная правилами, — объявление, шаги, длительность словами — едет полем проекции.
  Русский язык — склонение по числу, знак перед числом и слова правил, которые произносят обе
  стороны, — правилом не является и предметной областью тоже: он ушёл из `core/shared/language.ts` в
  `src/shared/language.ts`, названный в правиле границ исключением. По-русски говорят обе стороны, и
  вторая копия склонения разошлась бы с первой.

## Этапы

Каждый этап — работающее приложение и зелёный `npm run verify`. Полуготового промежутка нет.

**Э1. Договор и провод, чтения ещё локальные. Сделано.**

- **Э1а. Формат хранения.** Запись журнала носит идентификатор попытки, хранилище его читает и
  пишет. Поле необязательное, поэтому версия формата не выросла и прежние сохранения открываются
  как раньше. Пишущий пришёл следующим шагом; формат обязан был лечь раньше, иначе первая же
  записанная попытка не прочиталась бы на предыдущей сборке.
- **Э1б+Э1в. Ядро отвечает на сообщения, отображение переходит на дверь.** Словари отдают значения
  и функции сужения; `Clock` стал `Occasion` с идентификатором попытки; появились `presentation/`,
  `composition.ts` и часы в инфраструктуре; стор стал зеркалом ядра, `apply` исчез, места вызова
  стали командами. Снимок несёт версию и журнал; персонаж и каталог приезжают временной дверью
  сборки, потому что проекций ещё нет.

Дальше эти два шага не делятся: обязательный идентификатор в `Occasion` заставляет отображение его
выдавать, а выдавать его нечем, пока оно не перешло на команды. Постоянный идентификатор на всю
сессию сделал бы все записи одной попыткой, и первая же команда сочлась бы повтором. Функции сужения
до контроллера тоже никто не импортирует, а живость экспортов ядра проверяется.

**Э2. Проекции, по проекции на шаг.** `Character.of(…)` уходит из отображения — 13 файлов.
Доступность, цена в ячейках, боевая роль, вердикт проверки концентрации, урон с учётом уровня
переезжают в `core/presentation/views/` и приезжают полями снимка.

Шаг — проекция со всеми местами, которые она гасит, а не экран: свёрнутые величины показывают и
«Игра», и «Сумка», и «Лист», и переносить их «для одного экрана» значит писать в двух других
временную обвязку и снести её на следующем шаге. Порядок — от самой изолированной проекции к самой
разделяемой.

- **Лист. Сделано.** `sheetView` несёт справочные поля, здоровье, разбор Класса Доспеха,
  постоянные вклады и шесть характеристик с навыками. Шторка характеристики уехала вместе с ним:
  имя характеристики приезжает словом правил, и держать рядом её же типом значило бы сузить слово
  в отображении. Подписи ищутся по слову правил, а не берутся ключом словаря.
- **Сумка. Сделано.** `bagView` сводит вещь с её запасом — до проекции их не сводил никто, потому
  что «что это такое» и «сколько этого у меня» принадлежат разным владельцам. Заодно ушёл отбор
  прибавок по категории вещи: их снимает владелец вещи при записи, а список повторял ту же
  проверку у себя.
- **Книга и боевой список. Сделано.** `spellRowsView` несёт строку целиком: роль, цену момента,
  урон с учётом уровня, применимость в обстановке и причину отказа. Причина берётся у того же
  способа сотворения, который предложит мастер применения, — порознь они однажды назвали разные
  причины одного запрета. Обстановку проекция выводит сама по журналу: второй ответ на «идёт ли
  бой» разошёлся бы с первым молча. Числа заклинателя стоят раз на персонажа, а не при каждой
  строке. `canCastNow` исчез: вердикт и объяснение теперь одно поле.
- **Игра и Привал. Сделано.** `resourcesView` несёт ячейки по возрастанию, руны, очки, ручную
  поправку защиты и числа начала хода; хиты и Класс Доспеха повторно не считаются — их даёт лист.
  `concentrationView` несёт раунд начала, длительность словами правил, урон по потраченной ячейке,
  спасбросок и вердикт проверки, которой ответил последний урон: вердикт едет перечислением, а не
  выводится на экране из граней кости. Проверка выводится из журнала, а не из ответа на команду —
  снимок обязан складываться из одного состояния, иначе перечитанная сессия покажет не то, что
  показывала до перезагрузки; ради этого запись журнала носит урон. Строка обзавелась защитой «если
  сотворить», объявлением и шагами: способ выбирают правила, и спросить их дважды значит однажды
  получить два разных ответа на один вопрос. Подробная карточка уехала тем же шагом.

**Э3. Отображение перестаёт считать. Сделано.** Снимок состоит из проекций с самого начала — состояние в нём
не ездило никогда. Временная дверь устроена иначе: стор держит рядом со снимком живую сессию ядра,
и экраны считают по ней. Пока дверь открыта, отображение вправе вывести что угодно само, и включать
правило границ не на чем. Э3 закрывает её.

Шаг — по-прежнему проекция со всеми местами, которые она гасит, а не экран. Порядок — от самой
изолированной к самой разделяемой.

- **Час, руна, восстановление. Сделано.** `recoveryView` несёт то, что вернут час и конец боя, и
  причину, по которой операция сейчас не идёт: причину называет тот же сценарий, которым откажет
  нажатие, — до этого шага погашенная кнопка и отказ говорили разными словами, и час обещал одно, а
  отвечал другое. Доступность руны уехала в `resourcesView`, и он стал читать сессию, а не
  персонажа: непотраченная реакция видна только по журналу.
- **Экономия хода. Сделано.** `turnView` ехал снимком с Э1, но экраны его не читали и выводили
  экономию сами. Свойство `economy` виджетов стало полем `turn` проекции: строка кровавой магии,
  оба мастера и три экрана. Контекст применимости черновика пока собирается из той же проекции — он
  уходит вместе с самим контекстом следующим шагом.
- **Правка листа: вопрос. Сделано.** Договор получил четвёртый вид сообщения (ADR-0044):
  `questions.ts`, `ask` в порте, клиенте и обоих проводах, `previewer.ts` в представлении, третий
  маршрут у сетевого провода. Здоровье и уровень спрашивают им то, чего в состоянии нет, и больше
  не принимают персонажа целиком — только своё поле листа. Шаг встал раньше мастера применения,
  чем стоял в плане: объявление мастера зависит от выбранной ячейки, то есть от черновика, и без
  вопроса мастеру уезжать не на чем.
- **Мастер применения и кровавая магия. Сделано.** Способы сотворения приехали строкой заклинания:
  ячейка, её цена, урон именно этой ячейкой и вердикт — у каждого свой, потому что потраченная
  первая ячейка не запрещает вторую, а один вердикт на строку не отвечает ни на один вопрос игрока.
  Список способов непуст по форме договора: заклинание, которое сотворить нечем, называет ту ячейку,
  которой его сотворяли бы (FR-248). Объявление, шаги, эффект руны и границы броска костей зависят
  ещё и от набранного и потому спрашиваются вопросом: прислать их с каждым способом значило бы
  прислать сорок текстов ради одного показанного. Кровавая магия уехала целиком — курс ступени и
  границы сделки проекцией, цена набранного и объявление вопросом; строка списка перестала
  сокращать чужую фразу и называет ту же, какой откажет подтверждение. Оплата описана одной схемой
  на команду, проекцию и вопрос: показанный способ — ровно то, чем игрок потом и заплатит.
- **Карточка. Сделано.** Строка несёт карточку: школу, английское имя, полные правила, повышение уровня,
  тактический совет, цель, эффекты успеха и провала, триггер реакции, материальный компонент,
  реплику с жестом и заметку игрока. Гасит подробную карточку, шторку реакций, список покупок и
  подпись цели — все четверо сегодня берут карточку из каталога сессии.
- **Схема ритуала. Сделано.** Схема приезжает начерченной — кольцами, штрихами, знаками и подписью, — а не
  словарём знаков: закрытый список знаков принадлежит контенту и стережёт его разбор, а перо
  принадлежит экрану.
- **Отыгрыш. Сделано.** Варианты с любимыми, отключёнными и ротацией — полем карточки: они выводятся из
  состояния, и второй счётчик ротации разошёлся бы с первым молча.
- **Действующее и привал. Сделано.** Активные эффекты — проекцией со сроком, напоминанием и вкладом в
  защиту. Магическое восстановление устроено как правка листа: возвращаемое приезжает проекцией,
  набранный план спрашивается вопросом; сегодня и бюджет, и предел по уровню проверяет шторка.
- **Правки листа и сумки. Сделано.** Шторки берут начальное значение из листа и сумки, а не из состояния;
  вещь приезжает целиком — база доспеха, прибавки, цена и заметка.
- **Журнал и обмен. Сделано.** Журнал показывается записями снимка. Выгрузка спрашивается вопросом: в ней
  время, которого в состоянии нет. Разбор присланного файла уезжает в ядро, а то, чьими карточками
  идёт игра, становится полем снимка.
- **Дверь закрывается. Сделано.** `session`, `spellCatalog` и `spellCatalogSource` уходят из
  `sessionStore.ts`, `LiveReader` и `readLive` — из его зависимостей, `live()` — из сборки ядра.
  Оболочка ждёт снимок, а не сессию. Часы ушли из сторов следом: время выгрузки и записей ставит
  ядро, а показывающему оно не нужно ни для чего. Вместе с дверью из отображения исчезли
  `CharacterState`, `Spell` и прочие типы состояния — принимать их стало некому.

**Э4. Правило включается. Сделано.** Правило в `check-layers.py` названо девятнадцатым, исключения
перечислены поимённо. Из ядра рабочие файлы отображения брали три вещи, и все три сняты:

- **Механика русского языка. Сделано.** Модуль целиком уехал в `src/shared/language.ts` — отдельным
  корнем, который не знает никого. Не только склонение и знак числа: сокращение монеты, размер кости,
  имена спасбросков и фигуры области произносят обе стороны, и вторая копия любого из этих слов
  разошлась бы с первой молча. Заодно проверка владения сокращениями стала сверяться с новым адресом,
  а покрытие — считаться и по этому корню: ошибка в морфологии видна за столом как ошибка приложения.
- **Перечни для полей выбора. Сделано.** Едут проекцией (ADR-0045): открытый вопрос закрыт в её
  пользу, потому что исключение под перечни было бы четвёртым, а исключение, которое нельзя назвать
  поимённо, и есть отсутствие правила. `choicesView` несёт величины листа, размеры существа,
  категории вещи и доспеха, монеты, степени владения, ступени истощения, цели руны и границы уровня
  со значением характеристики. Величина приезжает разобранной — чем имя составлено, — иначе подпись
  разбирала бы `save:dexterity` сама и разошлась бы с составителем имени.
- **Руны мастера применения. Сделано.** Список, подписи и «выбирает ли руна цель» уехали полем
  предпросмотра рядом с эффектами, перечень целей — проекцией. Правило спрашивалось дважды: мастер
  показывал выбор цели, а черновик по тому же правилу цель сбрасывал. Подписи целей остались за
  экраном, и `RUNE_TARGET_LABEL` ушёл из ядра — там их не произносит никто.

Исключений три: композиционный корень, модуль языка и прогонная сборка сторов — та собирает настоящее
ядро на хранилище в памяти, и подделка вместо него проверяла бы подделку. С этого момента протечка
обратно невозможна.

**Э5. Сеть как дым-тест.** Маршруты Next, `httpTransport`, переменная окружения. Одна партия,
сыгранная через `NEXT_PUBLIC_ARCANE_BACKEND=http`, доказывает шов целиком.

Э1 и Э5 независимы от Э2 по файлам; Э3 читает результат Э2, Э4 — результат Э3.

**Чего требует прогон.** Покрытие ядра держится на 100 %, поэтому контроллер с его четырьмя
десятками ветвей приезжает вместе со своим прогоном, а не после него. Живость экспортов проверяется
только внутри `src/core/**`: `contract/` живёт без импортёров и прогон не ломает, а вот первый же
модуль в `core/presentation/` обязан быть кем-то позван — маршрутом, сборкой или отображением.
Отсюда и неделимость Э1: контроллер, сборка, стор и места вызова садятся одним куском.

## Что уезжает из отображения

Инвентаризация по фактам, а не по памяти:

- 53 места вызова `apply(current => …)`, из них 18 в «Игре» и 18 в «Привале»;
- 13 файлов с `Character.of(…)`: КД и его разбор, предел подготовленных, модификаторы спасбросков,
  ручная поправка КД, свёрнутые величины листа;
- `canCastNow`, `isSpellReady`, `ritualAvailable`, `castableInSituation` — доступность;
- `slotPriceOf`, `combatRoleOf` — цена в ячейках и боевая роль становятся полями проекции строки;
- `checkOutcome`, `effectiveDamage`, `MINIMUM_CONCENTRATION_DC`, `startRound` — сводка концентрации;
- `systemClock` — из провайдера отображения в инфраструктуру ядра.

Список закрыт целиком: `effectiveDamage` держался дольше всех — мастер применения считал им урон
каждой предложенной ячейки, — и уехал полем способа сотворения.

Э2 закрыл список, но не отображение: сверх него по правилам считали экономия хода, доступность руны
защиты, возврат хитов по окончании боя, причина недоступности восстановления ячеек, три числа часа,
способы сотворения с их ценой и вердиктом и весь тариф кровавого колдовства. Они не черновик — все
выводятся из состояния, и все разобраны шагами Э3.

Остаётся то, что правилами не является и по CLAUDE.md отображению и принадлежит: выбор слов, порядок
строк, отбор по уже посчитанному признаку, состояние экрана — открытые шторки, черновик мастера,
режим. Эталон формы — подпись к проверке концентрации: вердикт приходит из правила, файл выбирает
фразу. Он уже написан правильно; меняется одно — вердикт приедет перечислением по проводу.

## Открытые вопросы

- Идентификатор сессии. Сегодня сессия одна и подразумеваемая. Серверу нужна названная. Вводится на
  Э5 вместе с маршрутами; до тех пор локальный провод обходится единственной.
- Версия снимка и расхождение. Снимок несёт `version`; что делает клиент, увидев версию новее
  ожидаемой, решается на Э5 — до сети расхождение невозможно.
- Сборка сетевого варианта. `output: "export"` не исполняет обработчиков маршрутов (ADR-0002),
  значит Э5 — отдельная сборка без статического экспорта. Чем она отличается (флаг, второй конфиг)
  и где живёт бэкенд — решается на Э5; офлайн-PWA остаётся основной поставкой в любом случае.
