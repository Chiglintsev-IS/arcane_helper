# Ограниченные контексты

Карта предметной области. Здесь живёт **идея**: какие понятия существуют, что про них всегда верно и
кто кому что говорит. Реализация — в коде; этот каталог её не пересказывает.

## Зачем это разделение

Приложение ведёт персонажа за игровым столом, а стол занимается разным: бьётся, отдыхает, готовит
заклинания, варит зелья, записывает встреченных тварей. Правила у всего этого свои, и пока они лежали
одним плоским состоянием, каждое новое требование приходилось дописывать в четыре места и помнить про
пятое.

Ограниченный контекст — граница, внутри которой слово значит одно и то же. «Книга» в контексте
заклинаний — список известного; «книга» в бестиарии — записи о тварях. Разные слова, разные модели, и
смешивать их нельзя.

## Что контекстом не является

**«Бой» и «Вне боя» — ситуации интерфейса, а не контексты.** Модель у них одна: те же ресурсы, то же
здоровье, те же эффекты. Два контекста с одной моделью неизбежно ссылались бы друг на друга — это и
есть цикл. Домен здесь один — **Схватка**: раунды и экономия хода. Что показывать в каждой ситуации,
решает [интерфейс](../screens.md).

**Экран персонажа — тоже не контекст.** Это вид на контекст «Персонаж».

## Карта

Сегодня существуют:

| Контекст | Код | Отвечает на вопрос | Корень агрегата |
|---|---|---|---|
| [Каталог](catalog.md) | `catalog` | Что вообще бывает по правилам | `Spell` |
| [Персонаж](character.md) | `character` | Кто он: уровень, характеристики, производные числа | `CharacterSheet` |
| [Книга заклинаний](spellbook.md) | `spellbook` | Что персонаж знает и что подготовил | `Spellbook` |
| [Магические ресурсы](arcana.md) | `arcana` | Чем платить за сотворение | `Arcana` |
| [Жизнеспособность](vitality.md) | `vitality` | Сколько выдержит и чем платит кровью | `Vitality` |
| [Эффекты](effects.md) | `effects` | Что действует и что держится вниманием | `EffectBoard` |
| [Схватка](encounter.md) | `encounter` | Идёт ли бой, какой раунд, чем ещё можно сходить | `Encounter` |
| [Снаряжение](equipment.md) | `equipment` | Что у персонажа есть вещественно | `Equipment` |
| [Журнал изменений](journal.md) | `journal` | Что случилось и как это вернуть | `Journal` |

Спроектированы, но не реализованы:

| Контекст | Код | Отвечает на вопрос | Вырастает из |
|---|---|---|---|
| Инвентарь | `inventory` | Что несёт, сколько весит, где лежит | `equipment` |
| Профессии | `crafting` | Что можно изготовить и из чего. Первой — алхимия | `inventory` + `catalog` |
| Развитие | `progression` | Чему учится, что открывается на уровне | `character` + `catalog` |
| Свод знаний | `codex` | Твари, расы, устройство мира, карта — записи игрока | ни из чего, независим |
| Хроника | `chronicle` | Что происходило в путешествии: сессии, места, решения | ни из чего, независим |

**Журнал изменений и Хроника — разные контексты, и это не спор о словах.** Журнал существует ради
обратимости: он хранит снимки состояния, ограничен по глубине и вытесняет старое. Хроника ведётся
игроком, живёт вечно и снимков не хранит. Одно слово «журнал» на двоих привело бы к тому, что глубина
100 однажды съела бы историю кампании.

## Направление зависимостей

Циклов нет: связи идут строго слева направо, и ни одна не возвращается.

```
                        ┌───────────┐
                        │  catalog  │  справочник правил
                        └─────┬─────┘
                              │
        ┌───────────┐         │         ┌───────────┐
        │ character │◄────────┼────────►│  journal  │  снимки для отмены
        └─────┬─────┘         │         └─────▲─────┘
              │               │               │
   ┌──────────┼───────────────┼───────────┐   │
   ▼          ▼               ▼           ▼   │
spellbook  arcana         vitality     effects│
   │                                          │
   ▼                                          │
equipment                              encounter (проекция журнала)
```

Читается так: `catalog` и `journal` не зависят ни от кого. `character` — общее ядро: его читают, но
не меняют. Остальные контексты зависят только от `character` и `catalog`. `encounter` выводится из
`journal`, но журнал про схватку не знает.

Три правила, из которых всё остальное выводится:

1. **Контекст не знает соседа.** Книга не знает про ячейки: она отвечает «что я умею», а не «чем
   оплачу». Ресурсы не знают про кровь, кровь не знает про ячейки. Сводят их
   [сценарии](../scenarios.md) — и только они.
2. **Логика не знает про отображение.** Ни React, ни хранилища, ни режима экрана внутри контекста.
   Признак «идёт бой» приходит от схватки, а не от открытой вкладки.
3. **Состояние меняется только через корень агрегата.** Снаружи нельзя дописать эффект в массив или
   уменьшить остаток: инвариант проверяется в одном месте, а не у каждого вызывающего.

Правила проверяются, а не подразумеваются:

```bash
npm run check:layers
```

## Кто чем владеет

Состояние персонажа целиком разложено по агрегатам, пересечений нет.

| Данные | Владелец |
|---|---|
| Ячейки, магическое восстановление, руны, очки заклинаний | `arcana` |
| Хиты, временные хиты, снижение максимума, Кости хитов, подавление | `vitality` |
| Активные эффекты, концентрация | `effects` |
| Заговоры, книга, подготовленное, заметки, предпочтения отыгрыша | `spellbook` |
| Фокусировка, мешочек, дорогие компоненты | `equipment` |
| Записи об изменениях состояния | `journal` |
| Идёт ли бой, номер раунда, потраченное в ходу | `encounter` — выводится из журнала, не хранится |
| Имя, класс, уровень, характеристики, КС, слагаемые КД, профиль отыгрыша | `character` — за сессию не меняется |

Контекст «Персонаж» — общее ядро (shared kernel): то, что читают все, но не меняет никто. Уровень
нужен и ячейкам, и крови, и рунам; передавать его параметром честнее, чем копировать в каждый агрегат.

Режим экрана владельца среди контекстов не имеет: это состояние интерфейса, и правила от него
зависеть не вправе.

## Как контексты общаются

Внутри одного подтверждения — синхронно, через прикладной сценарий: всё живёт одной записью в
хранилище, распределённых транзакций нет, а одно нажатие обязано давать одну запись журнала.

**Событие** — способ рассказать о случившемся тем, кто про команду знать не должен. Начало хода не
знает про эффекты и регенерацию: оно объявляет факт, а истечение раундовых эффектов и восстановление
хитов — реакции на него.

События именуются в прошедшем времени и описывают факт, а не намерение: «ячейка потрачена», а не
«потратить ячейку». Команда — наоборот.

## Порты

Контекст объявляет, что ему нужно от мира; мир это реализует.

| Порт | Зачем | Реализация |
|---|---|---|
| `Clock` | Время события. Чистая функция его не изобретает | системные часы, фиксированные в тестах |
| `IdGenerator` | Идентификатор записи и эффекта | `crypto.randomUUID` |
| `SessionRepository` | Прочитать и записать сессию целиком | IndexedDB через Dexie; память в тестах |
| `SpellCatalog` | Найти карточку по идентификатору | встроенный каталог сборки; загруженный игроком |

Порта «бросить кубик» нет и не будет: бросает игрок за столом, приложение принимает результат.

## Как писать эти документы

Доменный документ описывает идею и переживает переименование функции. Поэтому в нём **запрещены**
имена функций и файлов, сигнатуры, пути `src/…`, номера строк и история правок — всё это живёт в
коде и в истории git, где обновляется само.

Разделы одинаковы у всех:

```
# Название (кодовое имя)
## Зачем это за столом      — 2–3 фразы языком игрока
## Язык                     — термины контекста; общие термины — в глоссарии
## Модель                   — агрегат, сущности, объекты-значения: смысл, не поля
## Инварианты               — нумерованный список «всегда» и «никогда»
## Правила                  — механика словами; числа — в коде под тестом
## Команды                  — что можно попросить: предусловие → следствие → событие
## События
## Границы                  — чего контекст не знает
## Требования               — список FR
```

Раздел «Границы» обязателен: именно он не даёт контексту расползтись.
