# Приёмка и тестирование

> Обновлено: 2026-08-02 · Статус: согласовано · Источник: ТЗ §20–21

## Критерии приёмки MVP

MVP считается готовым, когда все 20 критериев имеют статус `Проверено`. Статус ставится по результату
теста или игровой сессии, а не по факту написания кода.

| ID | Критерий | Требования | Статус |
|---|---|---|---|
| AC-01 | Приложение устанавливается на главный экран iPhone | [F-12](features/F-12-offline-pwa.md) | План |
| AC-02 | Приложение работает в авиарежиме | [NFR-001](features/F-12-offline-pwa.md#nfr-001), [NFR-002](features/F-12-offline-pwa.md#nfr-002) | План |
| AC-03 | Внесены 4 заговора и 25 заклинаний Торна | [FR-011](features/F-02-spell-card.md#fr-011), [content.md](content.md) | Проверено |
| AC-04 | Пользователь может выбрать 11 подготовленных заклинаний | [FR-101](features/F-09-preparation.md#fr-101) | Проверено |
| AC-05 | Заговоры не входят в лимит подготовки и не тратят ячейки | [FR-102](features/F-09-preparation.md#fr-102), [FR-072](features/F-06-resources.md#fr-072) | Проверено |
| AC-06 | Ритуалы отображаются отдельно и работают без подготовки | [FR-103](features/F-09-preparation.md#fr-103), [FR-073](features/F-06-resources.md#fr-073) | Проверено |
| AC-07 | Заклинания фильтруются по времени применения | [FR-002](features/F-01-combat-screen.md#fr-002) | Проверено |
| AC-08 | Реакции доступны через отдельный быстрый экран | [FR-060](features/F-05-reactions.md#fr-060) | Проверено |
| AC-09 | Каждое заклинание имеет техническую инструкцию | [FR-040](features/F-03-cast-wizard.md#fr-040) | Проверено |
| AC-10 | Каждое боевое заклинание имеет минимум три варианта отыгрыша | [FR-050](features/F-04-roleplay.md#fr-050) | Проверено |
| AC-11 | При использовании заклинания расходуется правильная ячейка | [FR-023](features/F-03-cast-wizard.md#fr-023), [FR-070](features/F-06-resources.md#fr-070) | Проверено |
| AC-12 | Повышение уровня корректно меняет параметры | [FR-071](features/F-06-resources.md#fr-071), [FR-041](features/F-03-cast-wizard.md#fr-041) | Проверено |
| AC-13 | Второе концентрационное заклинание вызывает предупреждение | [FR-081](features/F-07-concentration.md#fr-081) | Проверено |
| AC-14 | Активная концентрация и активные эффекты постоянно видны | [FR-082](features/F-07-concentration.md#fr-082), [FR-090](features/F-08-active-effects.md#fr-090), [FR-001](features/F-01-combat-screen.md#fr-001) | Проверено |
| AC-15 | Проверка концентрации рассчитывается по полученному урону | [FR-083](features/F-07-concentration.md#fr-083), [FR-084](features/F-07-concentration.md#fr-084) | Проверено |
| AC-16 | Долгий отдых восстанавливает ресурсы | [FR-130](features/F-06-resources.md#fr-130), [FR-131](features/F-06-resources.md#fr-131) | Проверено |
| AC-17 | Последнее действие можно отменить | [FR-111](features/F-10-journal-undo.md#fr-111) | Проверено |
| AC-18 | Состояние сохраняется после закрытия браузера и обновления | [NFR-003](features/F-12-offline-pwa.md#nfr-003) | Проверено |
| AC-19 | Данные можно экспортировать и импортировать | [FR-120](features/F-11-data-io.md#fr-120), [FR-121](features/F-11-data-io.md#fr-121) | Проверено |
| AC-20 | Механический и художественный текст визуально разделены | [FR-042](features/F-03-cast-wizard.md#fr-042), [FR-054](features/F-04-roleplay.md#fr-054) | Проверено |

Прогон, доказывающий каждый критерий, назван поимённо в [матрице трассировки](#матрица-трассировки).

**Чего не хватает AC-01.** Установку на главный экран не выполняет ни один прогон: её закрывает
игровая сессия. Приложение собирается статическим экспортом и отдаёт манифест с иконками, но проверка
«поставилось и открылось с иконки» остаётся ручной.

**Чего не хватает AC-02.** E2E-прогон `serves the app from cache when the network is gone` проверяет
не весь критерий: он глушит сеть маршрутом и убеждается, что запрос страницы отвечает из кэша.
Перезагрузку под перехватом сети WebKit не выполняет, поэтому открытие приложения с нуля в авиарежиме
проверяется руками, на телефоне.

**Уточнение AC-03.** Числа взяты из состава книги ([content.md](content.md)) и держатся тестом
`состоит из 29 карточек: 4 заговора и 25 заклинаний по уровням`. В ТЗ было 18 заклинаний — состав
расширен игроком, и критерий следует за книгой, а не за исходной цифрой.

**Уточнение AC-10.** Минимум три варианта отыгрыша проверяются по одному прогону на карточку:
`«Луч холода» имеет минимум контента по FR-050` и ещё 28 таких же. Управление вариантами
([FR-053](features/F-04-roleplay.md#fr-053)) в критерий не входит и остаётся несделанным.

**Уточнение AC-20.** Блок отыгрыша содержит ровно одну механическую строку — что из отыгрыша
обязательно по правилам ([ADR-0011](decisions.md#adr-0011)). Всё остальное в нём художественное, и
оформление у блока заведомо другое.

## Модульные тесты

Обязательно покрываются: расчёт КС заклинаний; расчёт атаки заклинанием; расходование ячеек; повышение
уровня заклинания; лимит подготовки; ритуальное применение; переключение концентрации; расчёт КС
концентрации; восстановление после отдыха; доступность реакции.

Движок правил ([rules-engine.md](rules-engine.md)) покрывается полностью: это единственное место, где
ошибка означает неверно проведённую игру, и она не проявится как сбой — только как неправильное число.

Отдельно применяется property-based тестирование там, где важна симметрия операций:

- любая последовательность списаний и возвратов ячеек возвращает исходное состояние;
- применение действия и его отмена дают состояние, побитово равное исходному;
- цикл экспорт → импорт даёт идентичное состояние.

## Интеграционные тесты

Сценарии: применение обычного заклинания; применение заговора; применение реакции; применение ритуала;
применение повышенной ячейкой; замена концентрации; отмена последнего действия; восстановление
состояния после перезапуска.

Дополнительно проверяются инварианты, которые нельзя проверить unit-тестом:

- до подтверждения в мастере состояние персонажа не изменилось ([FR-022](features/F-03-cast-wizard.md#fr-022));
- подтверждение применяет все пять эффектов атомарно ([FR-023](features/F-03-cast-wizard.md#fr-023));
- провал импорта не изменяет существующие данные ([FR-122](features/F-11-data-io.md#fr-122));
- обновление версии приложения сохраняет пользовательские данные ([NFR-003](features/F-12-offline-pwa.md#nfr-003)).

## E2E-тесты

Сценарии [UC-01…UC-05](use-cases.md) выполняются в мобильном viewport iPhone SE в WebKit. Он
обязательный: на нём проверяется требование «ключевая механика без прокрутки». Остальные размеры
добавляются по мере надобности — пока вёрстка одна, три прогона одного и того же сценария только
удлиняют сборку.

Отдельный прогон работы без сети ([NFR-001](features/F-12-offline-pwa.md#nfr-001)) проверяет ответ из
кэша при заглушённой сети, а не полное открытие приложения в офлайне: перезагрузку под перехватом
WebKit не выполняет. Разницу закрывает игровая сессия (AC-02).

Измеряемое в E2E:

- число нажатий до технической инструкции ≤ 2 ([M-02](product.md#метрики));
- число основных шагов применения ≤ 4 ([M-03](product.md#метрики));
- путь от триггера реакции до результата ≤ 2 нажатий.

## Проверка контента

Скрипт в CI, а не ручная вычитка, проверяет:

- все 29 карточек проходят Zod-схему, включая инварианты из [domain-model.md](domain-model.md);
- минимум художественного контента [FR-050](features/F-04-roleplay.md#fr-050) выполнен для каждого;
- в текстах отыгрыша нет стоп-слов из `prohibitedThemes` (огонь и производные);
- реплики не длиннее `maximumPhraseLength`;
- `announcementTemplate` не содержит текста из `roleplay` ([FR-042](features/F-03-cast-wizard.md#fr-042));
- ключи `damage.scaling` не ниже уровня заклинания;
- каждый идентификатор в подготовленном наборе существует в книге заклинаний.

## Доступность

axe-core прогоняется в E2E на семи экранах — бой, карточка заклинания, мастер применения, привал,
экран реакций, журнал, магическое восстановление — с наборами правил `wcag2a`, `wcag2aa`, `wcag21a`,
`wcag21aa`. Всё это один прогон `combat screen, spell card and wizard pass axe-core`. Первый прогон
нашёл настоящую проблему: цветные метки и кнопки не давали контраста 4.5:1, потому что цвета были
подобраны под тёмную тему. Отсюда тёмные варианты цветов для светлой темы в `globals.css`.

Плюс ручные проверки, которые axe не покрывает:

- информация различима без цвета — монохромный режим;
- зоны нажатия ≥ 44 × 44 px, включая элементы списков;
- вёрстка не ломается при увеличенном системном шрифте;
- тёмная тема читаема.

## Игровое тестирование

MVP должен пройти минимум две реальные игровые сессии. Это единственная проверка, которую невозможно
автоматизировать, и главный источник исправлений.

После каждой сессии фиксируются: действия, занявшие слишком много времени; непонятные формулировки;
недостающие предупреждения; лишние шаги; ошибки в механике; неиспользуемые элементы интерфейса.

Результаты сессии превращаются в изменения спеки (новые или изменённые `FR`), а не в правки кода
напрямую — иначе спека перестанет отражать приложение после первой же игры.

## Матрица трассировки

Обратная связь «требование → чем проверено». Строка есть у каждого требования в статусе `Готово` или
`Проверено`; названия прогонов приведены дословно, как они записаны в коде. Полный перечень способов
проверки указан в разделе «Проверка» каждой фичи.

Колонка «Unit» — прогоны движка правил, схем и контента (`src/rules`, `src/data`, `src/diagram`),
«Интеграционный» — прогоны состояния и интерфейса (`src/store`, `src/components`), «E2E» —
`e2e/uc-01-cast-spell.spec.ts`.

| Требование | Unit | Интеграционный | E2E | Критерий |
|---|---|---|---|---|
| FR-001 | — | `показывает все три вида экономии, когда все три есть в списке (FR-001)` | `combat-screen renders all resource blocks` | AC-14 |
| FR-002 | `без свободных ячеек оставляет только заговоры` | `показывает переключатель на каждый вид, который в книге есть` | `filter by casting time` | AC-07 |
| FR-003 | `действие плюс заговор оставляют только заговоры действием` | `значения одной категории соединяются «или»` | — | AC-07 |
| FR-010 | — | `показывает время, цену, дальность и пересказ эффекта` | `technical instruction is two taps away` | — |
| FR-011 | `состоит из 29 карточек: 4 заговора и 25 заклинаний по уровням` · `все карточки проходят схему и имеют уникальные идентификаторы` | `открывается по строке списка и показывает механику` | — | AC-03 |
| FR-012 | — | `сохраняется в состоянии и не попадает в журнал` | `technical instruction is two taps away` | — |
| FR-020 | — | `«Сотворить» открывает мастер применения` | `wizard steps order and cast spends the slot` | — |
| FR-021 | — | `типовое боевое заклинание проходится за два шага` | `wizard steps order and cast spends the slot` | AC-11 |
| FR-022 | — | `полный проход мастера не трогает ни персонажа, ни журнал` · `полный проход мастера и отмена оставляют состояние прежним` | — | AC-11 |
| FR-023 | — | `списывает ячейку и заводит эффект` · `списывает выбранную ячейку, создаёт эффект и одну запись журнала` | `wizard steps order and cast spends the slot` | AC-11 |
| FR-030 | `собирает все предупреждения, а не первое` · `предупреждает о неподготовленном заклинании из книги` · `согласован с проверкой доступности мастера применения (FR-030)` | — | — | — |
| FR-031 | `«Применить всё равно» доступно, пока среди предупреждений нет замены концентрации` | `«Применить всё равно» доводит применение до конца и показывает долг ячейки` | — | AC-11 |
| FR-032 | `называет бросок атаки готовым числом, а не модификатором` | — | `wizard steps order and cast spends the slot` | — |
| FR-033 | `минуты предупреждают о цене по правилам и называют время` · `минуты и часы называются числом: «1 минута», а не «Минуты»` | `накладывание дольше хода называет точное время, а не категорию (FR-033)` | — | — |
| FR-040 | `используют только известные подстановки` · `не оставляет незаполненных подстановок ни в одной карточке контента` | — | `technical instruction is two taps away` | AC-09 |
| FR-041 | `называет выбранный уровень ячейки при повышении` | `называет выбранный уровень ячейки и цель` | — | AC-12 |
| FR-042 | `ни в одном объявлении нет художественного текста` · `отклоняет объявление с художественным текстом` | `механика и отыгрыш живут в разных блоках (AC-20)` | — | AC-20 |
| FR-050 | `«Луч холода» имеет минимум контента по FR-050` (по прогону на каждую из 29 карточек) | — | — | AC-10 |
| FR-060 | — | `вход одним нажатием, вопрос о событии первым` | `reactions in one tap` | AC-08 |
| FR-061 | `предлагает только те события, на которые есть ответ` · `на успешный бросок врага ответить нечем: «Искусная острота» в книгу не вошла` | — | — | — |
| FR-062 | — | `событие находит своё заклинание и называет изменённое число (FR-062)` | `reactions in one tap` | — |
| FR-063 | — | `реакция возвращается началом следующего хода, а не концом раунда` · `израсходованная реакция не прячет варианты, а объясняет причину (FR-031)` | `reaction shows when it returns` | AC-08 |
| FR-070 | `даёт Торну 4 / 3 / 3 / 1` | `проходит схему и содержит подтверждённые числа` | `combat-screen renders all resource blocks` | AC-11 |
| FR-071 | `симметричен: любая последовательность списаний и возвратов обратима` | `возврат ошибочной ячейки (FR-071)` · `плитка ячейки открывает правку и возвращает списанное` | — | AC-12 |
| FR-072 | `consumesSlot заговор не расходует ячейку (FR-072)` | `применение заклинания (FR-023) заговор не расходует ячейку (FR-072)` | — | AC-05 |
| FR-073 | `consumesSlot ритуал не расходует ячейку (FR-073)` | `применение заклинания (FR-023) ритуал не расходует ячейку (FR-073)` | — | AC-06 |
| FR-080 | — | `подтверждённая замена оставляет ровно одну концентрацию (UC-03)` · `после замены остаётся ровно одна концентрация (FR-080)` | — | AC-13 |
| FR-081 | `предупреждает о замене концентрации и называет текущий эффект` | `вторая концентрация без подтверждения отклоняется` · `требует выбора из двух вариантов, а не «всё равно»` | `concentration block explains the effect` | AC-13 |
| FR-082 | — | `без концентрации карточки нет вовсе` · `показывает активную концентрацию карточкой с механикой (FR-084)` | `combat-screen renders all resource blocks` | AC-14 |
| FR-083 | `concentrationCheckDc урон 22 даёт КС 11` · `checkGuidanceRu называет наименьший проходящий бросок` | `при активной концентрации предлагает проверку с готовой КС` | `concentration block explains the effect` | AC-15 |
| FR-084 | `описывает заклинание с областью и без спасброска` | `показывает название, ячейку, механику и чем сорвётся` | `concentration block explains the effect` | AC-14, AC-15 |
| FR-090 | — | `короткий срок переводится в раунды: за столом считают ими` · `списывает ячейку и заводит эффект` | `wizard steps order and cast spends the slot` | AC-14 |
| FR-091 | — | `ручное завершение убирает эффект` · `закрывает неконцентрационный эффект и пишет это в журнал` | — | — |
| FR-092 | — | `напоминает о ежеходной работе, пока эффект держится` | — | — |
| FR-093 | `«Доспехи мага» заменяют базу: 13 + 2 + 2 = 17` · `считает КД так, как будто выбранное заклинание уже действует` | `КД меняется после применения «Доспехов мага»: 14 → 17 (FR-093)` | `combat-screen renders all resource blocks` | AC-14 |
| FR-094 | — | `начало хода снимает истёкшее: «Щит» держится один раунд` · `«Щит» сам исчезает с началом следующего хода, КД возвращается к 14 (FR-094)` | — | — |
| FR-100 | — | `заклинания вне книги подготовить нельзя (FR-100)` · `отмечает и снимает подготовку прямо в списке` | `book mode prepares spells` | — |
| FR-101 | `лимит подготовки равен 11` | `набор Торна начинается ровно на пределе: 11 из 11 (FR-101)` · `двенадцатое заклинание упирается в лимит и объясняет причину (FR-101)` | `book mode prepares spells` | AC-04 |
| FR-102 | `фильтр «подготовлено» скрывает снятое с подготовки, но не заговоры (AC-05)` | `заговор не готовится: он вне лимита и доступен всегда (FR-102)` · `считает подготовленное и не считает заговоры (FR-102)` | — | AC-05 |
| FR-103 | `для неподготовленного ритуала выбирает ритуал, а не ячейку (FR-103)` | `ритуалы не входят в подготовленные (FR-103)` · `неподготовленный ритуал не объясняется подготовкой (FR-103)` | — | AC-06 |
| FR-110 | — | `событие без изменения ресурсов всё равно записывается` · `записи содержат идентификатор заклинания и уровень ячейки` | — | — |
| FR-111 | — | `возвращает состояние побитово` · `отмена из журнала возвращает потраченную ячейку` | — | AC-17 |
| FR-112 | — | `не растёт бесконечно` | — | — |
| FR-113 | — | `свежее сверху` · `кнопка отмены только на верхней записи` · `пустой журнал объясняет, а не показывает кнопку` | — | AC-17 |
| FR-114 | — | `в «Бою», «Вне боя» и «Книге» кнопки отмены нет` | `undo returns the slot through the journal screen` | AC-17 |
| FR-120 | `выгружает персонажа целиком и все карточки` | `своя выгрузка загружается обратно и восстанавливает ресурсы (FR-120)` | — | AC-19 |
| FR-121 | `принимает собственную выгрузку` · `ошибка называет путь до поля, а не «ошибка импорта»` | `битый файл называет причину и состояние не трогает (FR-121, FR-122)` | — | AC-19 |
| FR-122 | `полная замена ставит состояние из файла` | `битый файл называет причину и состояние не трогает (FR-121, FR-122)` | — | AC-19 |
| FR-130 | — | `долгий отдых восстанавливает ячейки, руны и снимает концентрацию (FR-130)` | `camp mode reaches rest and recovery` | AC-16 |
| FR-131 | `applyArcaneRecovery возвращает выбранные ячейки` | `магическое восстановление работает один раз до долгого отдыха (FR-131)` · `магическое восстановление возвращает выбранные ячейки (FR-131)` | — | AC-16 |
| FR-132 | — | `короткий отдых ячейки не восстанавливает (FR-132)` · `короткий отдых доступен кнопкой и пишется в журнал` | — | — |
| FR-133 | — | `долгий отдых требует подтверждения и возвращает ячейки (FR-133)` · `отмена подтверждения ничего не меняет` | `camp mode reaches rest and recovery` | — |
| FR-140 | — | `начало хода восстанавливает действие и реакцию (FR-140)` · `«Мой ход начался» восстанавливает израсходованное` | `reaction shows when it returns` | — |
| FR-141 | — | `расходует действие, бонусное действие и реакцию` · `не даёт потратить бонусное действие дважды` | — | — |
| FR-142 | — | `ручное списание ячейки пишется в журнал и обратимо (FR-111)` · `плитка ячейки открывает правку и возвращает списанное` | — | — |
| FR-143 | `вне боя молчит: на привале минута ничего не стоит (FR-143)` | `вне боя действие не расходуется (FR-143)` · `учёт хода следует из режима, а не из переключателя (FR-143)` | — | — |
| FR-144 | — | `реакция, потраченная после начала хода, недоступна и сообщает когда вернётся` · `на израсходованную реакцию отвечает «когда вернётся», а не «нет» (FR-144)` | `reaction shows when it returns` | — |
| FR-150 | — | `проходит схему и содержит подтверждённые числа` · `за границы пула не выпускает` | — | — |
| FR-151 | — | `тратится вместе с ячейкой` · `без рун объясняет, когда они вернутся` | — | — |
| FR-152 | `руна войны округляет половину уровня вверх и не опускается ниже +1` · `пересчитывает эффект по уровню ячейки` | — | — | — |
| FR-153 | — | `спасают концентрацию: провал проверки можно не доводить до конца` · `провал спасброска отвечает руной, а не заклинанием (FR-153)` | — | — |
| FR-154 | — | `провал при доступной руне сначала предлагает Знаки ограждения` | — | — |
| FR-155 | — | `возвращает и тратит руну, записывая обе правки` · `руны правятся вручную и правка обратима (FR-111)` | — | — |
| FR-160 | `у каждого запрета есть причина словами` · `не содержит запрещённых мастером заклинаний` | — | — | — |
| FR-161 | `запрещённого нет в книге ни под русским, ни под английским названием` | — | — | — |
| FR-162 | — (отменено, см. фичу) | — | — | — |
| FR-170 | `exchangeHitPoints даёт целые очки и не тратит остаток` | `обменивает хиты на очки и снижает максимум` · `списывает хиты и максимум, начисляет очки и тратит действие` | `blood exchange goes through the wizard, not one tap` | — |
| FR-171 | `hitPointCost для Торна заклинание 1 уровня стоит 6 хитов` | `называет цену в хитах и остаток после обмена` | — | — |
| FR-172 | `снижает и текущее здоровье, и максимум на потраченное` | `списывает хиты и максимум, начисляет очки и тратит действие` | `blood exchange goes through the wizard, not one tap` | — |
| FR-173 | — | `возвращает не больше, чем утрачено кровавым колдовством` · `«Прошёл час» возвращает часть снижённого максимума (FR-173)` | — | — |
| FR-174 | `напоминает о ненужной проверке концентрации только при активной концентрации` | `говорит, что сделать, и что сказать мастеру` | — | — |
| FR-175 | `woundsFromExchange 3 созданных очков дают 2 ран` · `предупреждает о ранах, когда обмен опускает хиты в ноль` | `обмен в ноль предупреждает о ранах, но не запрещает` | — | — |
| FR-176 | — | `подавлено уроном огнём и солнцем (FR-176)` · `подавление солнцем названо причиной и проходится «Применить всё равно»` | — | — |
| FR-177 | `называет и хиты, и очки` | `до подтверждения хиты, максимум и очки не тронуты` | `blood exchange goes through the wizard, not one tap` | — |
| FR-178 | — | `создаёт запас на два заклинания одним действием` | `blood exchange goes through the wizard, not one tap` | — |
| FR-180 | — | `огненный урон подавляет особенности до начала хода` · `отмечает подавление особенностей огнём` | — | — |
| FR-181 | — | `признак солнца переключается и не переключается впустую` · `переключается из правки ресурсов и подавляет особенности` | — | — |
| FR-182 | — | `восстанавливает хиты и пишет величину в журнал` | — | — |
| FR-183 | — | `включённый признак виден значком в шапке, а не только внутри листа` · `отмечает подавление особенностей огнём` | — | — |
| FR-190 | `схема есть у каждого ритуала и только у ритуала` · `отклоняет ритуал без схемы` | `у заклинания без схемы не показывает ничего (FR-190)` | — | — |
| FR-191 | `отклоняет кольца не по убыванию` · `гептаграмма 7/3 — один обход через все семь вершин` | `рисует все кольца «Опознания»` | — | — |
| FR-192 | — | `показывает название ритуала, схему и подпись` · `карточка ритуала открывает схему на полный экран` | — | — |
| FR-193 | — | `подмена схемы не меняет результат применения` | — | — |
| FR-200 | — | `меняется без записи в журнал: отменять в виде нечего` · `начинает с боя и не показывает долгое накладывание` | `combat keeps the first card whole, the book keeps the header` | — |
| FR-201 | `бой — вся книга, кроме накладываемого минутами и часами` | `начинает с боя и не показывает долгое накладывание` | `combat keeps the first card whole, the book keeps the header` | — |
| FR-202 | `вне боя список пуст: там отдыхают, а не выбирают заклинание (FR-202)` | `«Вне боя» списка заклинаний не показывает вовсе (FR-202)` | `camp mode reaches rest and recovery` | — |
| FR-203 | `отбор по режиму (FR-201, FR-202, FR-203) книга не отбирает ничего` | `режимы экрана (FR-200, FR-201, FR-204) книга не отбирает ничего` | `book mode prepares spells` | — |
| FR-204 | — | `повторный выбор того же режима возвращает ту же сессию` · `режим попадает в состояние, а журнал не засоряет (FR-204)` | `state survives a reload` | — |
| FR-205 | — | `лечение поднимает текущие хиты и пишется в журнал` | — | — |
| FR-206 | — | `временные хиты записываются отдельным числом` | — | — |
| FR-207 | `«Магия крови» проходит фильтр действия и отсеивается фильтром реакции` | `стоит в бою среди заклинаний и подчиняется тем же фильтрам` | `blood exchange goes through the wizard, not one tap` | — |
| FR-208 | `в бою ритуального способа нет: +10 минут в раунд не помещаются (FR-208)` | `в бою тот же ритуал начинается ячейкой (FR-208)` | — | — |
| FR-209 | `содержит заговоры и подготовленные, но не остальное` | `подготовленное появляется в боевом списке (FR-209)` | `book mode prepares spells` | — |
| FR-210 | `реакции, затем цена, затем роль (FR-210)` · `порядок ключа: сначала реакция, потом цена, потом роль` | — | — | — |
| FR-211 | `атака называет бросок числом, а не словом «Атака»` | `разрешение называет число, а не вид броска (FR-211)` | — | — |
| FR-212 | `«Боевое» оставляет боевые` | `в бою — только время, роль и концентрация` | `filter by casting time` | — |
| FR-213 | `роль не выводится из урона: «Поглощение стихий» несёт урон и остаётся защитным` · `у каждой карточки указана роль в бою (FR-213)` | — | — | — |
| FR-214 | — | `готовит и снимает подготовку, записывая каждое действие в журнал` · `отмечает и снимает подготовку прямо в списке` | `book mode prepares spells` | — |
| FR-215 | — | `короткий отдых доступен кнопкой и пишется в журнал` | `camp mode reaches rest and recovery` | — |
| FR-216 | — | `поднимает здоровье до половины максимума` · `кнопка конца боя восстанавливает до половины максимума` | — | — |
| FR-220 | `журнал — списка заклинаний нет (FR-220)` | `переключение в «Журнал» показывает записи` · `шапка журнала показывает ячейки и не показывает экономию хода (FR-220)` | `undo returns the slot through the journal screen` | AC-14 |
| NFR-001 | — | — | `serves the app from cache when the network is gone` | AC-02 |
| NFR-002 | — | — | `serves the app from cache when the network is gone` | AC-02 |
| NFR-003 | — | `восстановление состояния после перезапуска` · `на сохранении новее версии сообщает про обновление приложения` | `state survives a reload` | AC-18 |

Строк нет у требований, которые ещё не сделаны: [FR-051](features/F-04-roleplay.md#fr-051)…[FR-054](features/F-04-roleplay.md#fr-054)
(отыгрыш) и [FR-184](features/F-16-troll-states.md#fr-184) (отложено). [FR-030](features/F-03-cast-wizard.md#fr-030),
[FR-041](features/F-03-cast-wizard.md#fr-041) и [FR-050](features/F-04-roleplay.md#fr-050) стоят не в
`Готово`: их строки показывают проверенную часть, а не полноту требования.

Названия с круглыми скобками при точечном прогоне нужно экранировать — `-t` принимает регулярное
выражение: `npx vitest run src/rules/slots.test.ts -t "заговор не расходует ячейку \(FR-072\)"`.
