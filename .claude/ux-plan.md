# План правок: интерфейс, компоненты, время, ремесло

> Составлен 2026-08-15 по итогам UX-аудита (`.claude/ux-audit.md`, 61 скриншот в `.claude/ux-audit/`)
> и интервью с игроком того же дня. Обновлён 2026-08-16 по итогам второго прогона работ.
>
> **Состояние `main`:** второй прогон влит целиком — волна 1, волна 2, этап 1 волны 6, плюс работа
> по второму разговору с игроком. `npm run verify:full` проходит: 130 файлов прогонов, 2034 прогона,
> 22 сценария браузера, партия через бэкенд.
>
> **`main` не отправлен в `origin`: 77 коммитов лежат только на машине разработчика.** Публикация
> идёт из `origin/main`, а он стоит на коммите до первой волны, — значит на телефоне игрока ничего
> из сделанного не видно. Это не состояние работы, а состояние публикации, и путать их дорого:
> «не вижу на экране» здесь означало «не отправлено», а не «не сделано».

Это рабочий план для исполнителей, а не документ спеки. Он называет файлы, и потому живёт в
`.claude/`, а не в `docs/`: путь `src/…` в документе домена запрещён.

**Карточка задачи может врать адресом.** Диагнозы аудита подтвердились все, а вот файлы за сутки
разъехались: причина погашенной кнопки жила не в том каталоге, названный комментарий-оговорка уже
исчез, числа контраста были сняты со скриншота и при замере разошлись. Адрес и число из карточки —
подсказка, а не факт: проверяй на месте, а расхождение называй в ответе.

---

## Брифинг исполнителя

**Один агент — одна задача — один коммит.** Карточка задачи ниже описана так, чтобы начать без
доуточнений: что делать, почему (момент стола и доказательство), какую строку спеки правит, какие
файлы трогает, чем проверяется.

### Что прочитать до первой строки кода

`CLAUDE.md` целиком, `docs/domains/README.md`, карточку своей задачи здесь и тот документ спеки,
который она правит. Больше ничего читать не нужно: если задача требует знания соседнего домена, это
названо в её карточке.

### Порядок внутри задачи

Тот же, что в `CLAUDE.md`, и отступать от него нельзя:

1. **Сначала спека.** Новое поведение появляется требованием в документе своего домена, потом кодом.
   Правка без строки в `docs/` — неполный коммит.
2. **Реализация вливается сразу.** Заработало — в `main`. Держать готовое невлитым до конца волны
   запрещено.
3. **Проверка названа.** `Готово` и `Проверено` без имени прогона в строке «Проверка» не бывает.
   Прогон должен существовать в коде — за этим следит `npm run check:docs`.
4. **Прогон, ловящий дефект, пишется до правки.** Написанный после, он доказывает только то, что код
   делает то, что делает. Убедись, что он падает на старом коде, и скажи об этом в ответе.
5. **Перед правкой экрана — навык `frontend-design`, после правки — скриншот 390×844 в тёмной теме.**
   Скрытие информации игрок трактует против: спрятал факт — обоснуй в сообщении коммита.
6. **Механику не выдумывать.** Число, КС, длительность, стоимость берутся из подтверждённого
   источника; при сомнении — запись в `docs/open-questions.md`, а не догадка в коде.
7. **Параллельное — в отдельных worktree** под `.claude/worktrees/`, слияние из каталога `main`.

### Правило игрока, старше прочих

Записано в `docs/ux.md`, раздел «Общие правила», и им судится каждая экранная задача:

> **Место и понятность — одно решение, а не два.** Факт вписывается в то, что уже стоит на экране, а
> не приписывается рядом второй строкой: дубль отнимает место и заставляет читать дважды одно и то
> же. Выбор «спрятать или дать вырасти» — признак, что решение не найдено: ищется третье, где факт
> назван целиком и не занял ни одной лишней строки.

Правило проверено на трёх задачах подряд, и трижды третье решение находилось в одном и том же месте:
**место занимал дубль.** Отказ в «Книге» повторял счёт, который стоял рядом; пять плашек вещи
повторяли одно и то же «+1»; имя шторки повторяло её заголовок. Убрали повтор — место нашлось,
и в двух случаях экран стал короче, а не длиннее. Приносить игроку выбор между «спрятать» и «дать
вырасти» — значит не доделать задачу.

### Ограничения прогона, которые стоили времени

- **E2E в worktree не запускать.** `playwright.config.ts` держит порты 4173/4174 с
  `reuseExistingServer: true` локально: два параллельных worktree дадут зелёный прогон против чужой
  сборки. Исполнитель гоняет `npm run verify`; `npm run verify:full` идёт на `main` после слияния и
  делает это тот, кто сливает. Прогоны браузера при этом **править обязательно**, если задача
  меняет подписи, — иначе они упадут у сливающего.
- **Офлайн-прогон чувствителен к нагрузке.** `serves the app from cache when the network is gone`
  ждёт service worker десять секунд; на загруженной машине это падает, на свободной проходит за
  0.6 с. Сам worker исправен — зонд показал активацию за 30 мс. Падение при живых параллельных
  задачах — не повод чинить приложение; перепроверь на тишине.
- **После ребейза гнать полный прогон, а не `check:docs`.** Две ветки меняли форму одного поля
  договора с разных сторон, git свёл их без конфликта, и разъехались они на типах: чистое слияние
  само по себе ничего не доказывает.
- **`node_modules` копировать только в свежий worktree** (`cp -Rc` с APFS — мгновенно). Копия в
  каталог, где они уже есть, вкладывает их внутрь себя, даёт две копии React и 224 «упавших»
  прогона, к коду отношения не имеющих.

### Номера идентификаторов раздаются заранее

За первый прогон работ номера столкнулись семь раз: пять на `FR`, два на `OQ`. Параллельные задачи
берут «следующий свободный» одновременно и друг друга не видят, а каждый развод стоит коммита или
ручного слияния. **Тот, кто раздаёт задачи, выдаёт каждой непересекающийся диапазон** `FR`, `OQ` и
`ADR` прямо в задании. Занято на конец второго прогона: `FR` по 316, `OQ` по 51, `ADR` по 0057.
Внутри диапазона свободны неиспользованные хвосты выданных: `FR-303`…`FR-305`, `FR-307`…`FR-309`,
`FR-312`…`FR-315`, `ADR-0058`…`ADR-0061` — их брать можно, они никем не заняты.

Правило сработало: за второй прогон ни одного столкновения номеров. Столкнулись зато **тексты**:
трижды подряд слияние вставало на том, что два исполнителя дописывали свою запись в конец одного
документа (`docs/agreements.md`, `docs/screens.md` дважды). Разводится это руками за минуту и потому
дешевле очереди, но раздающий должен закладывать эти минуты в каждое слияние.

### Шаблон задания

Агенту (`implementer` или общего назначения) даётся ровно это, с подставленным номером:

```
Ты работаешь в git worktree <путь>, ветка agent/<имя>. Работай только там. Ветку не
переключай, в main не сливай — слияние делает раздающий. node_modules уже стоят.

Задача З-NN из /абсолютный/путь/.claude/ux-plan.md (файл лежит вне worktree, читай по
абсолютному пути). Прочитай CLAUDE.md, docs/domains/README.md, правило подачи в
docs/ux.md («Общие правила»), карточку задачи З-NN целиком и тот документ спеки,
который она правит. Сделай ровно её и ничего сверх.

Порядок: строка требования в docs/ → код → прогон с именем из строки «Проверка».
Один коммит на законченную мысль, сообщение по-английски.

Экран трогаешь — сначала навык frontend-design, после правки скриншот 390×844 в тёмной
теме своим скриптом на выделенном тебе порту <N> (шаблон: <путь к shot-template.mjs>).
MCP-браузером не пользуйся: он один на все параллельные задачи.

Прогон: npm run verify. E2E не запускай — Playwright держит порты 4173/4174 с
reuseExistingServer, параллельные задачи дадут зелёный прогон против чужой сборки;
verify:full идёт на main после слияния. Но прогоны браузера, спрашивающие изменённые
подписи, поправь. Красный npm run verify — задача не сдана.

Свободный диапазон номеров: FR-<a>…FR-<b>, OQ-<c>. Их точно никто не займёт.

Параллельно идут: <перечень задач и их файлов>. В их файлы не лезь.

Остановись и верни вопрос, не делая правки, если: понадобилось игровое число, которого
нет в подтверждённом источнике; правка отменяет принятое ADR; понадобилось новое ребро
между контекстами.
```

**Что просить в ответе:** что изменил, номер требования, имя прогона, хеш коммита, путь к снимку,
итог `npm run verify`, и — отдельно — что сделал сверх карточки и почему, что отказался делать и
почему. Оба этих пункта за первый прогон работ оказались самыми ценными в ответах.

### Когда задача сделана

Прогон зелёный; требование в `docs/` стоит со статусом и названным прогоном; прогон с этим именем
существует; для экранных задач приложен скриншот; коммит в `main` один и читается как законченная
мысль.

### Чего не брать

Задачи с меткой `⏳` — они ждут данных, которых ещё нет. Задачи из волн, отмеченных как ждущие
другой волны, — порядок в таблице в конце документа.

### Что сделано на 2026-08-16

| Волна | Состояние |
|---|---|
| 0 — чинится сразу | **Закрыта.** Все тринадцать задач в `main`, плюс добор З-01б: правило «незаполненное поле» получило владельца, и шесть шторок правки перестали показывать английский Zod |
| 1 — бюджет экрана | **Закрыта.** З-24 (прогон первым, красный на старом коде) → З-20, З-21, З-22, З-23. Первая строка цела на 320 |
| 2 — компоненты и материалы | **Закрыта.** З-30…З-36 все в `main` |
| 3 — остатки времени | **Закрыта.** З-41, З-43, З-44; З-40 и З-42 отменены решением Р-5 |
| 4 — подача | Не начата, кроме З-54 (свёртка однородного): она про правду строки, а не про иерархию, и потому пошла раньше |
| 5 — навигация | Не начата |
| 6 — алхимия | **Этап 1 закрыт.** З-60 и З-61 в `main`: словарь свойств и ингредиент как знание. Этап 2 свободен |
| 7 — заметки | Отложена до волны 5 сознательно: восьмой ярлык в полосу, где не помещается семь, — вред, а не польза |
| 8 — способности предыстории | **Наполовину.** З-81 сделана; З-80 разблокирована — З-20 в `main`; З-82 поглощена волной 9 |
| 9 — лист к бумажному | **Закрыта.** З-95…З-98 |

Сверх волн сделано по двум разговорам с игроком — разделы «Решения игрока 2026-08-16» и «…второй
разговор» ниже. Из второго разговора в `main` легли: список покупок собирает и опустевшее (FR-302),
выход из нечитаемого сохранения (FR-310, FR-311), русская причина отказа во всём разборе (FR-316),
правило о знаменателе свойств (FR-306, `План`).

**Чего на экране нет и не должно быть пока:** «Ремесло» (З-62 ждёт ярлык «Ещё» из волны 5, поэтому
алхимия сейчас — домен и данные без экрана), заметки (волна 7 ждёт волну 5), нижняя панель (волна 5),
поиск и отбор (З-25…З-27), пул «Последней подсказки» (З-80), иерархия и цвет (волна 4).

### Очередь на сейчас

Рабочих каталогов под `.claude/worktrees/` нет — все убраны после слияния, ветки удалены; новый
каталог заводится под задачу и удаляется с ней. Свободно и ничем не занято:

| Что | Чем открылось |
|---|---|
| **З-25, З-26, З-27** — поиск, «Часто», роль в отборе | волна 1 отвоевала место, которое они займут; полоса фильтров держит два ряда на 320 и третьего не получит |
| **З-80** — «Последняя подсказка» пулом | З-20 в `main`: ряд прочих ресурсов перестроен, значку есть куда встать |
| **волна 5** — навигация вниз | ждала волну 1, она закрыта |
| **этап 2 волны 6** — калькулятор рецепта (З-63…З-65) | этап 1 в `main`: словарь свойств и знание об ингредиенте есть |

Дальше по порядку: волна 4 после волны 5; волна 7 после волны 5; экран «Ремесло» (З-62) после
«Ещё» из волны 5; этапы 3–4 волны 6 ждут ответов мастера.

### Открыто и ждёт игрока

Ничего из этого не блокирует работу: везде выбрано откатываемое поведение.

- **Тролль на нуле хитов больше не встаёт сам концом боя.** Инвариант жизнеспособности так и велел,
  но до 2026-08-16 код его не соблюдал, и за столом играли иначе. Смотреть в игре.
- **`OQ-44`** — при нечётном действующем максимуме два отрезка округляют половину по-разному.
  Числа расы самодельные, поэтому округление не выбрано за игрока.
- **Плитки шапки обещают «Правку»** там, где кнопка подтверждает: «Хиты 60/60. Правка: урон,
  лечение, временные». По FR-264 это спорно, но доступное имя плитки говорит, что откроется, а не
  как зовётся шторка. Отдельная правка отдельным требованием, если игрок сочтёт нужным.

Добавлено вторым прогоном:

- **Редкость свойства хранится у раскрытой ячейки**, как и велел план, и потому два ингредиента
  могут разойтись в редкости одного и того же свойства — никто их не остановит.
- **Имя поля в русской фразе — латинский идентификатор схемы**: «поле „hitPoints.maximumBase“»,
  «character: …». Закреплено прогоном FR-121, поэтому меняется только вместе с требованием: своего
  словаря имён полей у приложения нет.
- **Разбор на границе договора говорит по-английски и иначе не может**: договор не импортирует из
  `src/` ничего, а русские причины живут в основе домена. Видно это только сетевой поставке, где
  стороны могут разъехаться версиями. Либо словарь причин заводится внутри договора, либо
  записывается согласие, что нарушение договора игроку словами не объясняют.

---

## Решения игрока

Приняты в интервью 2026-08-15. Каждое ложится записью `ADR` в `docs/agreements.md` тем же коммитом,
что и первая задача, которую оно открывает.

### Р-1 ✅ Короткий отдых — десять минут

Правило стола. Записывается как домашняя инициатива (ADR-0026) и восстановление частями (ADR-0029):
запись называет правило и прямо отмечает, что база 2014 говорит «не короче часа», — чтобы через
полгода отличие не приняли за ошибку и не «починили» обратно.

Правится не число, а склейка: отдельного числа «1 час» в коде нет вовсе, короткий отдых
**тождествен часу по реализации** — `useCases/rest.ts:69-74` повторяет `useCases/health.ts:127-141`.

| Что отдых делал, потому что был часом | Как станет |
|---|---|
| Возвращал ступень кровавого снижения максимума (FR-173 — «за полный час») | Не возвращает |
| Гасил очки заклинаний (FR-175 — сгорают за час) | Не гасит |
| Гас в бою фразой «занимает час» | Фраза называет десять минут; запрет остаётся |
| Открывал магическое восстановление (ADR-0029), тратил Кости хитов | Без изменений |

→ **З-44**

### Р-2 ✅ Компоненты — три строки в механическом блоке и значки на строке списка

Механика возвращается в механический блок: голос, жест, материал — три отдельные строки, у материала
названа его судьба. На строке списка — три значка В/С/М, чтобы ответ был виден до открытия карточки.

Заменяет ADR-0011 (номер сохраняется, статус — `Отменено`). Причина, по которой строку прятали в
блок отыгрыша, исчезла: блок «Что сделать» стоит первым, пропустить его нельзя.

→ **З-30, З-31, З-35**

### Р-3 ✅ Отыгрыш переписывается под механику

Девять карточек противоречат своим же компонентам: у `message` (без голоса) «Торн произносит
несколько слов», у `counterspell` (без голоса) реплика «НЕТ!!! Не сегодня.», у `tashas-mind-whip`
(без соматики) жест предписан трижды; плюс `feather-fall`, `dimension-door`, `misty-step`,
`absorb-elements`, `hypnotic-pattern`, `rimes-binding-ice`.

Текст приводится к механике буквально: нет голоса — нет предписанной речи, нет соматики — нет
предписанного жеста. Правится и `docs/voice.md`: требование «возглас обязателен всем четырём
реакциям» не может стоять рядом с тремя реакциями, которые творятся молча.

→ **З-34**

### Р-3а ✅ Материал, закрытый фокусировкой, молчит в мастере и говорит тихо в карточке

В момент действия строки материала нет вовсе. В подробной карточке она стоит серой: «шерсть и
палочка — закрыто фокусировкой». Фокусировки нет — материал показывается и проверяется везде.
Дорогой и расходуемый материал показывается всегда: его фокусировка не заменяет.

**Побочно чинится дефект:** фокусировка записана дважды — флагом `spellcastingFocus` и надетой вещью
«Магическая фокусировка +1», и одна из другой не выводится. Источник — надетое, флаг уходит.

→ **З-32, З-36**

### Р-3б ✅ «Для чего нужна вещь» — производный факт

Вещь про своих потребителей не знает: это заклинание и рецепт называют, что им нужно, а список «куда
годится этот корень» собирается обходом каталога. Ингредиент остаётся просто ингредиентом.

На строке вещи появляется «Нужен для: Опознание · Поиск фамильяра». Список «Купить и носить»
переезжает с «Привала» в «Вещи» разделом «Чего не хватает» и показывает нули и отсутствующее —
включая закрытое фокусировкой, помеченное как несрочное: цель — понимать, что потенциально
потребуется, а не только то, что запрещает творить.

→ **З-33**

### Р-4 ✅ «Игра» разгружается на четыре блока

495 px хрома до первой строки в бою. Сворачиваются четыре: ряд значков в одну строку, блок
действующего в строку с раскрытием, ввод статуса в шторку, экономия хода в ряд кнопок. Ни одно число
не исчезает с экрана совсем — уходит за раскрытие. Итог ~300 px: первая карточка видна целиком на
320×568.

→ **З-20…З-24**

### Р-5 ✅ Время остаётся за игроком

Автоматического истечения минут, часов и дней не будет. Причина словами игрока: временное живёт не
только внутри игры, часть эффектов не должна исчезать сама, и ответственность остаётся за столом.
FR-094 сохраняется как есть: сами собой истекают только раунды.

**Следствие:** находка аудита «„Паутина“ пережила часовой отдых» закрывается не дефектом, а решением.
Задачи З-40 (истечение по событиям) и З-42 (остаток срока на строке) — **отменены**.

### Р-6 ✅ Алхимия — по справочнику стола; руны ждут мастера

Источник: «Алхимия Сумрачного Доминиона. Справочник игрока», редакция 31 июля 2026 — домашняя
система стола, не SRD. Копия лежит в корне репозитория.

**Граница возможного задана самим справочником:** он прямо говорит, что не содержит ни описаний
отдельных ингредиентов, ни механики конкретных эффектов. Значит приложение считает **сложность,
время, расходники и партию** — это полные формулы — и **не считает результат состава**: эффект
называется словом, его числа остаются столу. Ингредиенты и свойства **вводит игрок** по мере
открытия: мира приложение не знает, оно знает записанное.

Порядок работ выбран игроком: **журнал → калькулятор → изготовление → исследование**.

**Кузнечное дело и выковка рун правил не имеют** — ни в справочнике, ни в книге игрока (руны там
письменность, а не ремесло). Идут записью в `docs/open-questions.md`, кодом не трогаются.

→ **З-60…З-68**

### Р-7 ✅ Навигация — панель внизу, «Вещи» одним экраном

Полоса вверху не вмещает и семи ярлыков: 418 px содержимого при 296–366 доступных, два всегда за
краем; с ремеслом и заметками их девять. Панель переезжает вниз, в зону большого пальца: «Игра»,
«Книга», «Вещи», «Привал», «Журнал», «Ещё». Отмена остаётся в два нажатия, как её оценил ADR-0022;
«Ещё» держит «Лист», «Ремесло», «Заметки» и всё будущее.

«Вещи» — бывшие «Экипировка» и «Сумка» двумя разделами. Правило `docs/screens.md` «надеваемое и
счётное — два режима, а не два раздела одного» отменяется с обоснованием: оба экрана и так
помещаются целиком без прокрутки (501 и 541 px из 667), значит раздел даёт ту же изоляцию за один
ярлык.

→ **З-70…З-72**

### Р-8 ✅ Рамка остаётся только там, где что-то значит

Сейчас рамку носит всё: плитки, плашки, кнопки, разделы, поля, карточки. Единственная осмысленная
рамка — цвет роли заклинания (FR-211) — тонет среди декоративных. Группу держит отступ и подложка;
рамка остаётся роли. Побочно освобождаются пиксели: вложенные рамки с отступами и составляют часть
тех 495 px.

→ **З-50…З-55**

### Р-10 ⚠️ Предыстория «Нерадивый ученик» — механика есть, два расхождения

Игрок прислал предысторию 2026-08-15. Художественная часть кодом не трогается; механическая даёт
четыре вещи, и две из них спорят с уже записанным.

**1. Активная способность «Последняя подсказка» — это ресурс, а не текст.** Один раз до долгого
отдыха, при провале проверки Интеллекта, связанной с рунами, надписью, шифром, ритуалом,
головоломкой или магическим механизмом: бросок повторяется, к новому результату прибавляется бонус
мастерства, новый результат обязателен. Восстанавливается долгим отдыхом — ровно как руны и бюджет
магического восстановления, и живёт тем же пулом. → **З-80**

**2. Пассивная «Рунный почерк» — особенность без ресурса**: минута изучения записи отвечает, один ли
у двух записей автор, есть ли позднейшая вставка, менялась ли структура. Дома на «Листе» у неё нет:
раздела особенностей в модели нет вовсе — ни в домене персонажа, ни в домене листа. → **З-81**

**3. ✅ Два направления подтверждены, и источник приложению не нужен.** Предыстория даёт одно
владение, второе пришло из другого источника — игрок подтвердил, что причин две. Записывать
происхождение не требуется: от него не зависит ни одно правило приложения. Это тот случай, где
ADR-0049 не действует — он требует вещь или срок у **числовой прибавки**, а владение навыком стоит
рядом с владением спасброском, то есть фактом листа. В состояние идут два направления —
зельеварение и трансмутация; синтез ядов не изучен и изучаться не будет.

**4. ⚠️ Два имени у двух навыков.** Предыстория называет их «Анализ» и «Внимательность»; приложение
называет «Расследование» и «Восприятие», причём глоссарий это решил явно и прямо запрещает «проверку
Анализа». Одна идея — одно слово, поэтому третьего имени заводить нельзя: либо приложение переходит
на слова стола, либо предыстория читается через глоссарий. Решает игрок; до решения ничего не
меняется. → **З-82**

**5. Навыки предыстории не выбраны** — «Магия или Расследование» и «История или Восприятие» ждут
распределения. Игрок сказал: «статы распределю сам потом». Кодом это не блокирует ничего: владения
навыками уже правятся на «Листе» своей шторкой.

### Р-9 ✅ Заметки — текст и время, больше ничего

Свободная запись про мир: одно поле, отправка по «Ввод», свежее сверху, время в строке. Ни меток, ни
разделов, ни привязки к вещам — вводом в чужой ход платить нельзя. Поиск по слову приходит тем же
механизмом, что и поиск по заклинаниям (З-25). Заметка **на предмете** — отдельная вещь: у вещи поле
заметки уже есть и остаётся как есть.

### Р-11 ✅ Система — обычная 5e; правятся имена навыков и три факта листа

**Источник истины — фотография листа Торна от 2026-08-15**, и она отменяет предыдущее чтение книги
Доминиона. На листе шесть обычных характеристик и восемнадцать обычных навыков: ни «Удачи», ни
«Выносливости», ни «Единоборства», ни «Устрашения». Книга Доминиона в этой части — черновик
(расписана одна «Сила», у остальных пустые заголовки, «Бонус умения» и «Спасительные броски» стоят
словом «текст»), и за столом она не действует. ADR-0020 остаётся в силе целиком.

**Сверка подтвердила модель приложения.** Характеристики совпадают до числа: Сила 8, Ловкость 14,
Телосложение 16, Интеллект 18, Мудрость 12, Харизма 8. Спасброски листа (+8 Интеллект, +5 Мудрость,
+3 Ловкость, +4 Телосложение, 0 Сила, 0 Харизма) сходятся ровно при «Плаще защиты» с его +1 ко всем
шести — значит данные плаща верны. Инициатива +1 подтверждает домашнее правило ADR-0026: половина
суммы модификаторов Ловкости и Мудрости. Рукой на полях написано «Короткий 10мин» — правило Р-1.

**Расходится три вещи, и все три мелкие:**

| | На листе | В приложении | Что делаем |
|---|---|---|---|
| Имена трёх навыков | Аркана, Анализ, Внимательность | Магия, Расследование, Восприятие | приложение переходит на имена листа |
| Владения навыками | шесть: Аркана, Анализ, Природа, Внимательность, Ловкость рук, Выживание | четыре: без Ловкости рук и Выживания | добавить два |
| Размер | средний | крупный | править на средний |

Переименование дешевле, чем кажется: меняются подписи, а не ключи величин — миграция сохранений не
нужна. Но подписи живут не только на «Листе»: пассивная величина становится «пассивной
внимательностью», и карточки заклинаний, называющие проверки словами, правятся вместе с ними.
Глоссарий при этом меняет уже принятое решение — там прямо записано «не „проверка Анализа"», — и
поэтому нужен `ADR`: имя навыка приходит с листа игрока, потому что за столом он смотрит в лист и в
телефон попеременно.

→ **Волна 9**. Задача З-81 её больше не ждёт: состав «Листа» не меняется.

---

## Решения игрока 2026-08-16

Восемь ответов на вопросы, поднятые первым прогоном работ. Все реализованы и влиты; здесь они стоят
затем, чтобы следующий исполнитель не переоткрыл закрытое.

### Р-12 ✅ Регенерация идёт от максимума, который есть сейчас

Каждый раунд при хитах ниже половины **действующего** максимума — базы минус оба снижения. Порог
едет вниз вместе с каждой ступенью кровавого колдовства. Записано FR-270.

Правка вскрыла три дефекта, все исправлены: обожжённый не лечился коротким отдыхом (регенерация
спрашивалась до снятия подавления); конец боя лечил под прямым солнцем (подавление не спрашивалось
вовсе); ноль хитов не останавливал непрерывную регенерацию. **Последнее меняет игру** — см. «Открыто
и ждёт игрока».

### Р-13 ✅ Долгий отдых снимает отметки, заведённые руками

Ручной статус и поправка к КД — то, что игрок вписывает руками в блок действующего, — отдых не
переживают. Срок «пока держится заклинание» (рассеивание, вызванная сущность) переживает по-прежнему.
Снятое **называется** записью журнала: «Долгий отдых · „Отравлен“ истёк». `OQ-41` закрыт.

### Р-14 ✅ Условия у прибавки не будет, прибавки показываются все

Вещей, прибавляющих к части спасбросков, за этим столом нет; условие «против чего действует
прибавка» усложнило бы счёт всех величин ради случая, которого не бывает. `OQ-42` закрыт.

Отсюда же требование FR-271: вещь произносит своё число один раз, а за ним — всё, что оно двигает.
«Ещё N» из строки вещи убрано совсем.

### Р-15 ✅ Конец хода отмечается началом следующего

Отдельного нажатия у конца хода не будет. Срок «до конца следующего хода» отмеряется двумя отметками
начала; расхождение принято осознанно — в чужие ходы приложение скорее откажет лишний раз, чем
разрешит то, чего правило уже не даёт. `OQ-43` закрыт.

### Р-16 ✅ Молчащего нажатия не бывает

Поле, уехавшее с экрана, перестаёт быть обязательным: требовать число за раздел, которого на экране
нет, — требовать невозможного. Правка FR-253.

### Р-17 ✅ Отказ вписывается в счёт, а не растит заголовок

Длина отказа бралась из повтора: счётчик уже говорил «11 из 11», и отказ говорил это второй раз.
Убрали повтор у владельца — «11 из 11 · Снимите другое заклинание» укладывается в одну строку на
320 px. Правка FR-214, и она же — первый случай правила подачи.

### Р-18 ✅ Шторка называет своё дело заголовком

Общий глагол на кнопке («Подтвердить») читается быстро, но сам не говорит, что произойдёт; это
держится на заголовке. Заголовок и произносимое имя шторки — один факт, а не две строки. FR-274,
`Готово`: правило выполнено во всех тринадцати шторках.

### Р-19 ✅ Заметки — после переезда навигации

Волна 7 ждёт волну 5. Восьмой ярлык в полосу, где не помещается семь, испортил бы и место, и опыт.

---

## Решения игрока 2026-08-16, второй разговор

Четыре вопроса, поднятые вторым прогоном работ. Все закрыты и реализованы.

### Р-20 ✅ «Чего не хватает» — список покупок перед вылазкой

Раздел отвечает на вопрос «что купить», а не «чем я не могу сотворить сейчас». Поэтому вещь,
которую требует хотя бы одно заклинание, с нулём в сумке **переезжает** строкой в раздел и на это
время исчезает из своей категории: не копия, а переезд — дубля не возникает, а покупки собраны в
одном месте (FR-302).

Две границы проведены исполнителем и приняты: не переезжает то, чего не требует ни одно заклинание
(ноль «Комплекта болотной маскировки» — не покупка), и не переезжает закрытое фокусировкой — иначе
оно упало бы в приглушённый перечень имён и потеряло бы шторку, то есть единственный путь к
удалению. Строка незаведённой вещи не носит погашенной кнопки открытия: кнопки нет, а не есть
погашенная.

### Р-21 ✅ Телефон игрока — 390, но 320 остаётся обязательным

iPhone 13. Правило 320×568 держится не потому, что такой телефон есть, а потому, что этот предел
заставил волну 1 искать дубли вместо того, чтобы прятать: на 390 «свернём в строку» прошло бы без
сопротивления. Вырост строки «Мистической бодрости» в «Книге» на 320 (172 → 192 px) принят как есть:
там ничего не спрятано, а в «Игре» не выросло ничего ни на одной ширине.

### Р-22 ✅ Знаменателя приложение не обещает

`OQ-51` закрыт. Сколько у вида свойств всего, приложение не знает: справочник ставит потолок в
четыре и не даёт способа узнать, что следующего нет. Счёт называется без знаменателя — «раскрыто 2 ·
следующее не исследовано». Полный счёт появляется с отметкой игрока «свойств больше нет» и снимается
ею же: узнанное за столом бывает и ошибкой. Записано FR-306 со статусом `План` — кодом ляжет вместе
с экраном «Ремесло».

### Р-23 ✅ Старые ветки — по пользе, а не по дате

Игрок про ветки от 2026-08-04 не знал: это работа прогона до аудита. Разобраны по пользе.

- `task19` — влита как есть: сохранение с полем `toString` больше не проходит за поле состояния.
- `task20` — перенесена на сегодняшний код (слить было нельзя: хранилище сессии переписано).
  Нечитаемое сохранение больше не запирает: причина, сырая копия хранилища, и лишь за ней «начать
  заново» с подтверждением (FR-310, FR-311). Попутно найден дефект, из-за которого выход не работал
  бы вовсе: «начать заново» само падало тем же отказом, из которого выходят.
- `task18` — перенесена и доведена дальше образца: русскими стали не пять типов, а весь разбор.
  `russianSchemaErrors` больше не экспортируется, разбор идёт одним входом — написать новое место
  разбора без русских причин теперь просто нечем (FR-316). Шестое место разбора нашлось после
  слияния, в порту хранилища: непрочитанное сохранение объясняло себя по-английски на том самом
  экране, который только что завели.

Открытым по этой теме осталось два места, оба названы выше в «Открыто и ждёт игрока»: имя поля
латиницей в русской фразе и разбор на границе договора.

---

## Волна 0 — закрыта 2026-08-16

Карточки ниже оставлены как описание того, что было починено и почему: они объясняют момент стола,
который держат прогоны. Брать из них нечего — все тринадцать в `main`.

Идут параллельно, каждая в своём worktree.

### З-01 · Отказ ввода говорит по-русски и в месте ввода · P0 · S

**Доказательство:** «Хиты» → «Урон» → «Записать» с пустым полем печатает в верхнюю полосу сырой
Zod-JSON по-английски: `[{"expected":"number","code":"invalid_type","received":"NaN"}]`, полоса 105 px
сдвигает экран, поле не помечено ничем (`25-zod-error-empty-damage.png`).

**Момент стола:** по игроку попали, он торопится записать урон — и получает текст, с которым нечего
делать.

**Что сделать:** пустое поле — несобранная команда. Либо экран не отправляет её и называет причину
под полем («Наберите число урона»), либо владелец возвращает отказ словами. Верхняя полоса не
показывает текста, не написанного по-русски, ни при каком вводе.

**Спека:** `docs/ux.md`, раздел про отказы. **Файлы:** `src/ui/features/edit-hit-points/`,
`src/core/presentation/controller.ts`, `src/contract/commands.ts`. **Проверка:** компонентный
«пустое поле урона отказывает у поля, а не полосой».

### З-02 · Причина погашенной кнопки стоит на кнопке · P1 · S

**Доказательство:** «Магическое восстановление · осталось 4 уровня» погашено, причина «Берётся после
короткого отдыха» живёт только в `title` и `aria-label` — на телефоне наведения нет (`08-camp.png`).

**Что сделать:** причина второй строкой на самой кнопке. Заодно обойти все `title` на кнопках.
**Спека:** `docs/ux.md` — «Гасится то, чью причину видно рядом»; FR-131. **Файлы:**
`src/ui/features/arcane-recovery/`, `src/ui/widgets/camp/ui/Camp.tsx`. **Проверка:** компонентный
«причина недоступности видна без наведения».

### З-03 · Отказ приходит туда, где нажимали, и не двигает раскладку · P1 · M

**Доказательство:** «Книга», 11 из 11, «+» на y≈515 — ответ на y=85…160, список сдвигается вниз на
86 px: под пальцем другая строка (`33-prepare-over-limit.png`).

**Что сделать:** причина принадлежит закреплённому счётчику подготовки — он меняет подпись на «11 из
11 — сначала снимите другое» и подсвечивается. Верхняя полоса не двигает раскладку вообще: стоит
поверх содержимого, а не в потоке. **Спека:** FR-214. **Файлы:** `src/ui/screens/book/`,
`src/ui/app/`. **Проверка:** компонентный «отказ по лимиту не двигает список».

### З-04 · «Книга» объясняет, почему в бою не подготовить · P1 · S

**Доказательство:** в бою кнопок подготовки ноль, фильтра «Ритуал» нет, а счётчик «11 из 11»
остаётся и обещает подготовку; причины на экране нет (`31-book-in-combat.png`). **Что сделать:**
счётчик дописывает «подготовку меняют вне боя». **Спека:** FR-214, FR-217. **Файлы:**
`src/ui/screens/book/`. **Проверка:** компонентный «в бою счётчик называет причину».

### З-05 · На шаге объявления сначала то, что говорят вслух · P1 · S

**Доказательство:** шаг 2 мастера — 1008 px в окне 503 px; «СКАЗАТЬ МАСТЕРУ» начинается на y≈530, до
чтения вслух ~430 px прокрутки (`15-wizard-announce.png`). **Что сделать:** порядок шага — «Сказать
мастеру» → отыгрыш → «Что сделать» напоминанием. **Спека:** ADR-0010. **Файлы:**
`src/ui/widgets/cast-wizard/ui/CastWizard.tsx`. **Проверка:** компонентный «формулировка мастеру
видна без прокрутки».

### З-06 · Активная вкладка проходит AA · P2 · S

`rgb(49,134,233)` на `rgb(29,41,61)` = 3.98:1 при 14px/500 — единственный текст ниже AA в тёмной
теме. Поднять яркость либо притемнить подложку до `rgb(15,23,43)` (даёт 6.8:1). **Файлы:**
`src/ui/shared/`. **Проверка:** прогон контраста.

### З-07 · Метка чекбокса — 44 px · P2 · S

«Урон огнём» — метка 351×20 при правиле ≥44×44. Квадрат может остаться 20 px, высоту даёт метка.
**Файлы:** `src/ui/features/edit-hit-points/`. **Проверка:** прогон зон нажатия.

### З-08 · Шапка «Привала» свёрстана как шапка «Игры» · P2 · S

«Игра» — КД 164×44 от x=12; «Привал» — 184×44 от x=0, последняя ячейка упирается в край. Одинаковая
обёртка, поля 12 px. **Файлы:** `src/ui/widgets/resource-header/`, `src/ui/screens/rest/`.
**Проверка:** компонентный «шапка одинакова в «Игре» и «Привале»».

### З-09 · Нулевой пул гасит свой цвет · P2 · S

При обнулении всего плитки ячеек приглушаются, а «Руны 0/3» остаётся зелёной; «Очки 0» не отличается
от «Очки 5». Цвет — носитель «это руны», а не «руны есть». **Файлы:**
`src/ui/widgets/resource-header/`. **Проверка:** компонентный «нулевой пул подан как ноль».

### З-10 · Отмена называет, что вернулось · P2 · S

Запись исчезает молча, кнопка появляется у следующей в той же точке — второе касание отменяет
вторую операцию вслепую. На месте исчезнувшей записи — строка «Вернулось: ячейка 3 уровня».
Подтверждения не добавлять. **Спека:** FR-114. **Файлы:** `src/ui/widgets/journal/`.
**Проверка:** компонентный «отмена называет возвращённое».

### З-11 · Один глагол на одно действие · P2 · S

Пять глаголов фиксации: «Сохранить», «Записать», «Подтвердить», «Вернуть ячейки», «Отдохнуть», плюс
коллизия «Отменить» (вернуть сделанное) и «Отмена» (уйти). Остаются два: «Сохранить» — где правят
запись, «Подтвердить» — где операция меняет игру; в журнале «Отменить» → «Вернуть». Слова приходят
от владельца. **Спека:** `docs/glossary.md`, `docs/ux.md`. **Проверка:** прогон владения фразами.

### З-12 · Конец боя истекает раундовые эффекты · P1 · S

`combat_ended` эффектов не трогает (`useCases/turn.ts:79-95`) — раундовый эффект последнего раунда
доживает до следующего боя. Это не спорит с Р-5: раунды истекают сами по уже принятому FR-094, а
здесь они не истекают там, где должны. **Файлы:** `src/core/domain/effects/effectBoard.ts`,
`useCases/turn.ts`. **Проверка:** интеграционный «конец боя снимает раундовое».

### З-13 · Длительность контента не бывает без числа · P2 · S

`durationSchema` не требует `value` для `rounds`/`minutes`/`hours` — в отличие от `castingTimeSchema`
с тремя `.refine`; `{type:"hours"}` пройдёт схему и станет «до 0 часов». **Файлы:**
`src/core/domain/catalog/spell.ts`. **Проверка:** unit «длительность без числа отвергается».

---

## Волна 1 — бюджет экрана и путь к строке (Р-4) · пять задач закрыты 2026-08-16

**Все пять в `main`.** Первая строка списка начинается на 394 px вместо 570; хром от верха области
прокрутки — 309 px вместо 497 (Р-4 обещал «~300»). Замер на слитом `main`, 320×568: вне боя низ
первой карточки 530, в бою 560 при экране 568.

Порядок оказался важен буквально: З-24 писалась первой и была красной — 705 px против 568.

- **З-20 ✅ сделано, и наоборот карточке.** Карточка велела «постоянное остаётся в строке, редкое —
  за раскрытием»; замер показал, что четыре постоянных значка — 345 px при 296 доступных, то есть
  одной строкой они не встают в принципе, а прятать истощение и подавление — то самое скрытие,
  которое игрок трактует против. Третье решение нашлось там же, где всегда: **место занимал дубль** —
  шапка показывала пул дважды, плиткой и значком. Постоянное ушло в плитки, редкое осталось на виду.
  Правит FR-232, FR-261, FR-001.
- **З-21 ✅ сделано · Блок действующего — строка с раскрытием.** Шторка «Действует» поглотила
  `ConcentrationPanel` и `ConcentrationCard`. Правит FR-082, FR-084.
- **З-22 ✅ сделано · Ввод статуса уехал в ту же шторку.** Постоянное поле держало 44 px ради имени,
  которое набирают раз в бой. Правит FR-236, FR-001.
- **З-23 ✅ сделано наполовину по карточке · Числа встали на кнопки, которые их тратят.** Инициатива —
  на «Начать бой», раунд — на «Новый ход», остаток реакции — на «Реакции». В ряд кнопок вместилась
  только реакция: три кнопки со значками на 320 — 277 px из 296, и значки стоили бы второй строки,
  той самой, которую волна отвоёвывает. Действие и бонусное встали в строку действующего.
  Правит FR-001, FR-221, FR-232.
- **З-24 ✅ сделано · Прогон бюджета идёт на трёх ширинах** — 320, 375, 390, в бою при удерживаемой
  концентрации. Правит FR-218 (`Готово`).

### Дотянуться до нужной строки

До «Молнии» 1730 px прокрутки, пятнадцать строк, ни поиска, ни избранного: M-01 («нужное за пять
секунд») держится на памяти игрока.

**Жёсткое условие: фильтрация приходит без единого лишнего пикселя над списком.** Полоса фильтров
переносится на второй ряд, а не прокручивается, поэтому лишний переключатель стоит целого ряда — и
съедает ровно то, что отвоевали З-20…З-23.

- **З-25 · Поиск по названию.** Иконка в ряду кнопок, поле раскрывается поверх содержимого,
  закрывается по выбору строки, постоянной высоты не занимает. В «Игре» и в «Книге». Правит FR-002.
  **Файлы:** `src/ui/features/filter-spells/`, `src/ui/screens/game/`, `src/ui/screens/book/`.
  **Проверка:** E2E «поиск доводит до строки без прокрутки».
- **З-26 · Раздел «Часто».** Три–пять недавно применённых строк наверху списка, собираются из
  журнала: в бою повторяют то, что уже творили. **Файлы:**
  `src/core/presentation/views/spellRowsView.ts`, `src/ui/screens/game/`. **Проверка:** unit «часто
  применённое собирается из журнала».
- **З-27 · Роль отбирает и в «Игре».** FR-212 держит роль только в «Книге» с обоснованием «вопрос
  неспешного разбора». Первая половина обоснования спорна: FR-211 сам говорит, что роль отвечает на
  вопрос «зачем это нужно», а в бою это и есть вопрос — ударить или защититься. Вторая половина в
  силе, поэтому роль приходит **вместо** переключателя, а не в дополнение. Правит FR-002, FR-212.
  **Проверка:** компонентный «полоса фильтров «Игры» остаётся одним рядом на 320».

**Итог волны:** первая строка видна целиком на 320×568 при активной концентрации, и до любой строки
можно добраться, не прокручивая пятнадцать.

---

## Волна 2 — компоненты и материалы (Р-2, Р-3, Р-3а, Р-3б) · закрыта 2026-08-16

**Все шесть задач в `main`.** Материал стал вещью — наличие проверяется запасом в сумке,
расходуемый списывается сотворением и возвращается отменой, журнал называет вещь словами карточки
(FR-268). Фокусировка выводится из надетого, флага больше нет (FR-260). Отыгрыш приведён к механике
инвариантом схемы, восемь карточек вычитаны, `docs/voice.md` поправлен (FR-257).

Что вышло сверх карточек: **Р-2 просил три строки, а получилась одна** — и это тот же дубль, что и
везде. Голос и жест уже стояли императивами в перечне «Что сделать»; три строки «голос: обязателен»
в двух шагах от «Произнести вслух» повторяли бы их. Карточка З-30 при этом врала диагнозом:
компоненты подробная карточка показывала, не показывала она ровно двух вещей — материала, закрытого
фокусировкой, и того, чего заклинание **не** требует.

- **З-30 ✅ сделано · Компоненты видны в подробной карточке.** Одной строкой в блоке «Механика»:
  «голос · жест · жемчужина стоимостью не менее 100 зм (свой предмет)»; закрытое фокусировкой —
  приглушённым тоном. `costGp` в договор не поехал: цена уже стоит в тексте материала, и число рядом
  дало бы «100 зм — 100 зм». Правит FR-011 (`Готово`). **Проверка:** компонентный «подробная
  карточка называет три компонента (FR-011)».
- **З-31 ✅ сделано · Строка обязательного ушла из блока отыгрыша.** ADR-0011 отменён с сохранением
  номера, вместо него ADR-0054; блок отыгрыша художественный целиком (FR-290). `CastWizard.tsx`
  править не понадобилось — перечень приезжает из ядра готовым. **Проверка:** компонентный «в блоке
  отыгрыша нет механических строк (AC-20)».
- **З-32 ✅ сделано · Материал — вещь, а не флаг по заклинанию.** `materialsForSpellIds` — список
  идентификаторов **заклинаний**; расход при `consumed: true` не списывается; журнал пишет «Компонент
  куплен: identify». Материал становится вещью (`Items.idFromName` уже есть), наличие проверяется
  запасом, расходуемый списывается сотворением. Показ подчиняется Р-3а: под фокусировкой мастер
  молчит, карточка говорит серой строкой. **Файлы:** `src/core/domain/equipment/schema.ts`,
  `equipment.ts`, `src/core/application/casting/availability.ts`, `useCases/library.ts`,
  `src/core/domain/assembly/migration.ts`. **Проверка:** интеграционный «расходуемый компонент
  списывается сотворением и возвращается отменой».
- **З-33 ✅ сделано · «Требуется для» и «Чего не хватает».** Производный факт: вещь не знает своих
  потребителей, список собирается обходом каталога у владельца связи «компонент — вещь». Строка вещи
  называет, чем вещь требуется, — фактом в уже существующем перечне подробностей, третьей строки не
  появилось. Раздел «Чего не хватает» переехал с «Привала» в «Сумку» (FR-295, FR-296). Слово
  «Требуется для», а не «Нужен для»: приложение род не склоняет, а имена материалов бывают любого
  рода. Заведённая вещь с нулём в раздел нехватки не попадает — см. «Открыто и ждёт игрока».
  Закрытое фокусировкой названо перечнем имён: двенадцать строк дали бы ~840 px под то, чем Торн не
  пользуется.
- **З-34 ✅ сделано · Отыгрыш переписывается под механику.** Инвариант в схеме карточки и прогон в наборе
  контента: нет вербального — нет предписанной речи; нет соматики — нет предписанного жеста. Затем
  вычитка девяти карточек и правка `docs/voice.md` в части возгласа реакций. **Файлы:**
  `src/core/domain/catalog/spell.ts`, `docs/voice.md`,
  `src/core/infrastructure/catalog/thorne/spells/`. **Проверка:** unit «отыгрыш не предписывает того,
  чего не требуют компоненты».
- **З-35 ✅ сделано · Буквы В/С/М на строке списка.** Встали в угол имени, слева от подписи роли:
  «ВС Боевое», «СМ Другое». Буква стоит за требование, её отсутствие — за отсутствие требования;
  материал без замены фокусировкой выделен весом, а не девятым цветом. Три размещения были
  замерены на 320/375/390 в «Игре» и в «Книге»: угол имени — один выросший замер, ряд фактов — 19,
  ряд значков — 16. Единственный выросший назван в «Открыто и ждёт игрока». Правит FR-010, FR-211.
- **З-36 ✅ сделано · Фокусировка выводится из надетого.** Сейчас факт записан дважды: флагом
  `spellcastingFocus` и надетой вещью. Флаг уходит, источник — надетое. **Файлы:**
  `src/core/domain/equipment/`, `src/core/domain/assembly/migration.ts`. **Проверка:**
  интеграционный «снятая фокусировка возвращает требование материала».

---

## Волна 3 — что осталось от времени (Р-1, Р-5) · закрыта 2026-08-16

Все три задачи в `main`; З-40 и З-42 отменены решением Р-5. Что вышло сверх карточек:

- **З-41** развела сроки по тому, **чем срок кончится**, и таблица судеб полна по объявлению — новый
  срок без ответа на вопрос «переживает ли долгий отдых» не соберётся компилятором (FR-262). Заодно
  ушёл призрак: концентрационный эффект с особым сроком оставался на доске, когда саму концентрацию
  отдых уже снял.
- **З-43** отказалась заводить слово в чужом домене: подавление принадлежит жизненным силам, словарь
  сроков — эффектам, а нового ребра между контекстами задача не оправдывала. Срок отмеряется там, где
  живёт его результат (FR-266). Попутно найден настоящий дефект: подавление снималось первой же
  отметкой начала хода, то есть на целый ход раньше правила.
- **З-44** сняла с короткого отдыха всё, что он делал, потому что был часом, и записала домашнее
  правило ADR-0050 с прямой пометкой, что база 2014 требует «не короче часа».

Р-5 отменил автоматику: **З-40 и З-42 закрыты как ненужные**. Остаётся то, что не про отсчёт.

- **З-44 ✅ сделано · Короткий отдых перестаёт быть часом.** Число получает владельца в `vitality`, отдых
  перестаёт вызывать почасовое (возврат кровавого максимума по FR-173, сгорание очков по FR-175),
  фраза отказа в бою называет десять минут — и называется один раз, у владельца. **Файлы:**
  `src/core/domain/vitality/`, `src/core/application/useCases/rest.ts`, `docs/glossary.md`,
  `docs/scenarios.md` (FR-132), `docs/screens.md` (FR-215). **Проверка:** интеграционный «короткий
  отдых не делает того, что делает час».
- **З-41 ✅ сделано · `special` разделяется на смыслы** · P2. Сейчас это свалка из «до рассеивания», «пока
  держится сущность» и «снято вручную», и долгий отдых обращается с ними одинаково — ручной статус
  игрока переживает отдых заодно с фамильяром. **Файлы:** `src/core/domain/effects/schema.ts`,
  `effectBoard.ts`, `useCases/effects.ts`.
- **З-43 ✅ сделано · «До конца хода» становится выразимым сроком** · P2. Подавление огнём перестаёт быть
  приближением; снимается комментарий-оговорка в `vitality.ts:247`.

---

## Волна 4 — подача (Р-8) · ждёт волну 5, кроме сделанного

**З-54 сделана раньше очереди и это было верно:** она про правду строки, а не про иерархию, и
переделывать её вместе с волной 4 не придётся. Ушла дальше карточки — по ответу игрока (Р-14) «ещё N»
убрано из строки вещи совсем, а не только для однородного (FR-250, FR-265, FR-271).

**З-52 (цвет) получила данные, которых у карточки не было.** Сплошной прогон axe по семи режимам в
тёмной теме нашёл 18 нарушений контраста; три группы названы поимённо: `text-reaction` на плашках
`slate-900` — 4.45:1; чипы ролей на подцвеченных подложках — 4.31:1 и 4.02:1; `slate-500` на фоне
страницы — 4.23:1. Прогон axe сейчас **намеренно сужен полосой вкладок**: расширить его на всю тёмную
тему можно ровно тогда, когда эти три группы починены, — иначе он валит `verify:full`.

Красота здесь — дисциплина иерархии, а не украшение: экран, где всё одинаково важно, читается за
секунды только если важное крупнее.

- **З-50 · Рамка — носитель смысла.** Убрать рамку у группировок, держать группу отступом и
  подложкой; рамку оставить роли заклинания. **Файлы:** `src/ui/shared/`, виджеты. **Проверка:**
  скриншот до/после на 390 в тёмной теме.
- **З-51 · Герой экрана — число, которое произносят вслух.** КД, остаток хитов и ячеек, КС, урон
  крупнее и тяжелее подписей. Сейчас на карточке четыре уровня текста (16/500, 11/500, 11/400,
  12/400), и число, которое называют мастеру, не выделено ничем.
- **З-52 · Цвет — словарь из восьми занятых слов.** `docs/ux.md` раздал восемь оттенков смыслам, а
  плашки ресурсов берут те же краски для другого: «Руны» зелёные (цвет ритуала), «Раунд» синий (цвет
  действия). **Сначала замерить пиксельно** — вывод сделан по скриншоту, — затем либо развести, либо
  расширить палитру записью в `docs/ux.md`.
- **З-53 · Плотность и пустота стоят наоборот.** В «Игре» плотность избыточна, в «Экипировке» 166 px
  пустоты под содержимым, которое кончается на 501 px из 667.
- **З-54 ✅ сделано, и шире карточки · «Ещё N» не режет однородное.** У «Плаща защиты» семь фактов: КД +1 и +1 к каждому из шести
  спасбросков; строка показывает три и прячет четыре — при беглом чтении выходит «+1 к Силе и
  Ловкости». Свёртка однородного силами ядра: «Класс Доспеха +1 · Все спасброски +1». Это правило
  игры, а не оформление. Правит FR-250. **Файлы:** `src/core/domain/sheet/`,
  `src/core/presentation/views/`, `src/ui/entities/character/lib/itemMeta.ts`.
- **З-55 · Общая недоступность называется один раз.** Фраза «Недоступно: Действие уже израсходовано»
  повторена под 12 строками из 15, хотя та же причина стоит значком в шапке. Общая причина — строкой
  над списком; своя причина строки остаётся фразой. Правит `docs/ux.md`.

---

## Волна 5 — навигация (Р-7)

Идёт после волны 1: сначала экран разгружается, потом меняется способ переключения.

- **З-70 · Прототип панели и замер.** Шесть ячеек, скриншоты 320, 375, 390 в тёмной теме, проверка
  зоны нажатия и читаемости подписи при иконке. Требований не трогает — по итогам решение либо
  подтверждается, либо отменяется.
- **З-71 · Панель внизу вместо полосы вверху.** Правит FR-200, FR-204, `docs/ux.md` («навигация
  только вверху»), `docs/screens.md`. Режим по-прежнему переживает перезапуск и остаётся состоянием
  интерфейса. **Файлы:** `src/ui/app/`, `src/ui/features/screen-mode/`. **Проверка:** E2E «режим
  переживает перезапуск».
- **З-72 · «Вещи» одним режимом из двух разделов.** Правит FR-242, FR-249, правило `docs/screens.md`
  о двух режимах и ADR-0031 (запись не удаляется, статус меняется). Условие приёмки жёсткое: оба
  раздела помещаются без прокрутки на 320×568, иначе слияние отменяется. **Файлы:**
  `src/ui/screens/gear/`, `src/ui/screens/bag/`, `src/ui/widgets/gear/`, `src/ui/widgets/bag/`.

---

## Волна 6 — алхимия (Р-6)

Первым коммитом — `ADR` о расширении области MVP (`docs/constraints.md:88` требует его явно) и новый
домен в `docs/domains/`. Новый контекст `crafting` вписывает своё ребро в `scripts/check-layers.py`
отдельным коммитом. Вещь про ремесло не знает: `kind` остаётся скаляром, а «куда годится этот
корень» выводится обходом рецептов — тем же механизмом, что «Нужен для» в З-33.

### Этап 1 — журнал ингредиентов · закрыт 2026-08-16

Первым коммитом прошли ADR-0056 (расширение области MVP — продуктовая развилка, поэтому запись в
`docs/agreements.md`) и ADR-0057 (владение направлением живёт в ремесле, а не в свободном списке
строк персонажа), новый домен и ребро `crafting → catalog` отдельным коммитом. З-60 и З-61 легли
одним коммитом: единственный потребитель словаря — контекст ремесла, и коммит с одним словарём был
бы красным на проверке живых экспортов.

- **З-60 ✅ сделано · Закрытый словарь свойств.** 225 записей: 84 зельеварение, 84 синтез ядов,
  57 трансмутация. Неровный конец столбцов подтвердился — трансмутация кончается на 57-й строке,
  когда две другие идут до 84-й. Разбор шёл по ячейкам, а не по строкам, границы сверены глазами, и
  последняя запись каждого столбца закреплена прогоном: молчаливое усечение упадёт вслух (FR-300).
- **З-61 ✅ сделано · Ингредиент как знание, отдельное от запаса.** До четырёх упорядоченных ячеек,
  номер — глубина сокрытия; знание переживает исчезновение вещи. Тождество — по названию, без
  отдельного идентификатора: чужое правило именования пришлось бы копировать через границу, которую
  ремеслу пересекать нельзя (FR-301). Редкость хранится у раскрытой ячейки — со следствием, названным
  в «Открыто и ждёт игрока».
- **З-62 · Экран «Ремесло»** — режим под «Ещё» по Р-7. Ингредиент остаётся вещью в «Вещах» со своим
  счётом порций; в «Ремесле» живёт знание. На строке вещи — производная подпись «раскрыто 2 ·
  следующее не исследовано»: знаменателя приложение не обещает, пока игрок не отметил, что свойств
  больше нет (FR-306, ответ игрока по `OQ-51`). С отметкой подпись становится «раскрыто 2 из 2».

### Этап 2 — калькулятор рецепта

- **З-63 · Совпадения.** Выбраны 2–4 вида — приложение называет, какие свойства совпали (свойство
  входит при двух и более разных источниках), сколько у каждого источников и какая отсюда ступень:
  два источника — обычная, три — усиленная (+3 Сл, около ×1,5), четыре — концентрированная (+6 Сл,
  около ×2). Несколько порций одного вида совпадения не создают. **Главное правило безопасности из
  справочника — на экране:** пока состав не очищен, действуют **все** совпавшие свойства, а не
  только желаемое.
- **З-64 · Расчёт сложности.** База 10, итог не ниже 5. Восемь групп модификаторов: редкость
  основного и дополнительных эффектов, ступень усиления, длительность, начало действия,
  периодичность, цели и область, способ применения, сопротивление; затем очистка (+5) или подавление
  по редкости; затем ограничения и последствия с потолком −6. Всё считает ядро — экран не считает
  ничего.
- **З-65 · Итог и предел оснащения.** По итоговой сложности: время партии, класс и цена расходников
  за начатый час, число готовых единиц (порции + ⌊порции/4⌋). Оснащение бонуса не даёт — оно задаёт
  предел сложности и размер партии; работа без профильного набора добавляет +5 и делит партию вдвое.
  Сложность выше предела — отказ словами: «Сложность 32 выше предела набора: 30», а не погашенная
  кнопка.

### Этап 3 — изготовление

- **З-66 · Расход одной записью журнала.** Списывается по одной порции каждого вида на каждую
  рецептурную порцию. `adjustBagCount` меняет одну вещь за вызов и пишет свою запись — изготовление
  из трёх ингредиентов дало бы три отмены на одну операцию и вернуло бы половину рецепта.
- **З-67 · Проверка разработки и известный рецепт.** Бросок делает игрок, приложение принимает
  результат и сравнивает со сложностью (ADR-0021): 1к20 + профильный навык, если изучен, +
  модификатор Интеллекта. Натуральные 20 и 1 названы своими последствиями, критический провал —
  таблицей d6. После первого успеха рецепт записан и повторяется без броска, пока совпадают все
  четыре условия справочника; замена даже одного вида ингредиента — новая формула и новая проверка.

### Этап 4 — исследование свойств

- **З-68 · Раскрытие по порядку.** Сырая проба и поверхностный анализ для первого свойства; таблица
  «свойство → время, Сл, оснащение, расход порций» для второго-четвёртого, плюс модификатор редкости
  и расходники по классу за каждый начатый час. Третье и четвёртое требуют стационарной лаборатории.

### Владения Торна — ответ игрока 2026-08-15

Торн владеет **зельеварением** и **трансмутацией**; **синтезом ядов не владеет** и изучать его не
собирается. Бонус — обычный бонус мастерства, как и читается формулировка справочника «бонус умения
профильного навыка, если он изучен». Два направления подтверждены игроком (Р-10.3): предыстория даёт одно, второе пришло из
другого источника, и происхождение приложению не нужно.

**«Последняя подсказка» на алхимию не распространяется** — ответ игрока. Значит переброс проваленной
проверки в мастере изготовления и в исследовании не предлагается, и упоминания способности там нет:
подсказка про руны и загадки, а не про реторты.

Отсюда числа, и они проверяемы по листу: Интеллект 18 даёт модификатор +4, бонус мастерства на 7
уровне равен +3.

| Направление | Проверка Торна |
|---|---|
| Зельеварение | 1к20 + 3 + 4 = **1к20 + 7** |
| Трансмутация | 1к20 + 3 + 4 = **1к20 + 7** |
| Синтез ядов (не изучен) | 1к20 + 4 |
| Гибрид с участием ядов | 1к20 + 4 — справочник берёт **наименьший** бонус среди задействованных направлений |

Последнее — не мелочь, и экран обязан это называть: рецепт, куда затесалось ядовитое свойство,
роняет проверку на три. Игрок должен видеть причину до броска, а не после провала.

**Кому принадлежит владение направлением** — техническая развилка, решается `ADR` в
`docs/decisions.md` при первом коммите волны. Рекомендация: закрытый словарь из трёх направлений и
владение ими живут в контексте ремесла, а не в `tools` персонажа. `tools` — свободный список строк
(сейчас пустой), и правило, висящее на совпадении строки, разъедется молча; к тому же владение
направлением имеет смысл только внутри ремесла. Бонус мастерства и модификатор Интеллекта ремесло
читает у листа, как их читает применение заклинаний.

### Оснащение Торна — ответ игрока 2026-08-15

**Надёжные походные комплекты** по обоим изученным направлениям — набор зельевара и набор
трансмутатора, 180 зм за направление. Отсюда пределы, и они жёстче, чем кажется:

| Что задаёт оснащение | Значение у Торна |
|---|---|
| Предельная сложность | **20** — рецепт сложнее невозможен, а не «сложен» |
| Предел партии | **6** рецептурных порций → 6 + ⌊6/4⌋ = **7 единиц** состава |
| Доступное исследование | только **первое и второе** свойства; третье и четвёртое требуют стационарной лаборатории |

Это и делает калькулятор нужным: база 10 плюс редкий эффект (+5) плюс усиление тремя источниками
(+3) плюс минута длительности (+2) уже даёт 20 — предел. Отказ «Сложность 22 выше предела набора:
20» будет звучать часто, и он обязан называть, **чем именно** набрано лишнее, а не просто гаснуть.

**Синтез ядов Торну почти закрыт, и это надо показывать до броска, а не после.** Направление не
изучено — проверка теряет бонус мастерства, — и профильного набора токсиколога нет: справочник
добавляет +5 к сложности и делит предел партии вдвое. Вместе это −3 к броску и +5 к порогу, то есть
любое ядовитое свойство в смеси меняет расчёт вдвое сильнее, чем выглядит.

### Открытые вопросы, которые заводятся вместе с волной

Не блокируют этапы 1–2:
- механика конкретных эффектов: справочник её не содержит, поэтому приложение называет эффект
  словом и его чисел не знает — подтвердить, что это устраивает стол;
- кузнечное дело и выковка рун на предметах — правил нет вовсе.

---

## Волна 7 — заметки (Р-9) · ждёт волну 5 (Р-19)

- **З-90 · Свободные записи.** Режим под «Ещё»: одно поле ввода, отправка по «Ввод», свежее сверху,
  время в строке формата «ЧЧ:ММ» — тем же способом, каким журнал показывает событие. Ни меток, ни
  разделов. Запись правится и удаляется из своей строки; поиск приходит механизмом З-25.
  **Спека:** новый `FR` в `docs/screens.md` и запись в `docs/glossary.md` (заметка про мир против
  заметки на вещи — два разных слова). **Файлы:** новый экран в `src/ui/screens/`, новый контекст
  либо расширение журнала — решает `ADR`: журнал существует ради обратимости, и складывать в него
  то, что не отменяется, значит смешать две сущности. **Проверка:** компонентный «запись заводится
  одним полем и переживает перезапуск».

---

## Волна 8 — способности предыстории (Р-10) · наполовину

**З-81 сделана и влита** (FR-263): на «Листе» появилась карточка «Особенности» с «Рунным почерком».
Особенность хранит только название и то, что она даёт словами источника; механизма числового вклада
от особенности **сознательно не заведено** — величину двигают надетое и действующее, как требует
ADR-0049, а второй механизм появится вместе с первой особенностью, которой число вправду понадобится.
Кнопки правки у карточки нет: особенность приходит с происхождением.

**З-80 разблокирована:** З-20 в `main`, ряд прочих ресурсов перестроен. З-82 поглощена волной 9.

- **З-80 · «Последняя подсказка» — пул, восстанавливаемый долгим отдыхом.** Пул 1/1 рядом с рунами и
  очками: тем же способом хранится, тем же способом тратится, той же операцией возвращается.
  Показывается в ряду прочих ресурсов — и потому делается **после З-20**, иначе новый значок
  вернёт в шапку те пиксели, которые волна 1 отвоевала. Ноль — состояние: истраченная подсказка
  остаётся строкой с нулём. Границу применения задал игрок: способность про руны, надписи, шифры,
  ритуалы, головоломки и магические механизмы — **на алхимию она не распространяется**, и в мастере
  изготовления её не предлагают. **Спека:** новый `FR` в `docs/domains/arcana.md`. **Файлы:**
  `src/core/domain/arcana/schema.ts`, `arcana.ts`, `src/core/application/useCases/rest.ts`,
  `src/ui/widgets/resource-header/`. **Проверка:** интеграционный «подсказка тратится один раз и
  возвращается долгим отдыхом».
- **З-81 ✅ сделано · Особенности на «Листе».** Раздела особенностей нет ни в одном домене, а он нужен не
  только «Рунному почерку»: ADR-0049 уже говорит о вкладе «расовой чертой или особенностью», не имея
  для неё места. Особенность — название, что даёт, и **есть ли у неё игровое окончание**; та, что
  двигает число, приходит вкладом, как надетая вещь. «Рунный почерк» ничего не двигает — он
  справка, и это первый её представитель. **Спека:** новый `FR` в `docs/domains/character.md` и
  строка в `docs/screens.md` (FR-230 перечисляет состав «Листа»). **Файлы:**
  `src/core/domain/character/`, `src/ui/widgets/character-sheet/`.
- **З-82 · Одно имя навыку** — поглощена волной 9. Вопрос «Анализ или Расследование» перестал быть
  вопросом перевода: список навыков меняется целиком, и имена придут из него.

---

## Волна 9 — лист приводится к бумажному (Р-11) · закрыта 2026-08-16

Все четыре задачи в `main`. ADR-0051 отменил прежнее решение глоссария; переименование задело
**двадцать два файла — на семнадцать больше трёх, названных карточками**: имя навыка жило и в шапке
ресурсов (набранное там вторым разом, теперь приходит от владельца подписей), и в комментариях ядра,
и в заголовках прогонов. Три места оставлены нарочно — датированные записи о том, что было решено
тогда: отменённый ADR-0038, закрытый OQ-38 и архив планов. Историю не переписывают.

Мелкая волна с крупной ценой ошибки: тут правятся числа, которые игрок называет вслух. Идёт
параллельно всему, ничего не ждёт.

- **З-95 ✅ сделано · `ADR`: имя навыка приходит с листа игрока.** Отменяет прежнее решение глоссария, где прямо
  записано «не „проверка Анализа"». Обоснование: за столом игрок смотрит в бумажный лист и в телефон
  попеременно, и два имени одного навыка — лишний перевод в голове там, где на решение секунды.
  **Файлы:** `docs/decisions.md` или `docs/agreements.md`, `docs/glossary.md`.
- **З-96 ✅ сделано · Три навыка переименовываются.** «Магия» → «Аркана», «Расследование» → «Анализ»,
  «Восприятие» → «Внимательность»; вслед за третьим — «пассивная внимательность». Остальные
  пятнадцать имён с листом уже совпадают. Ключи величин не трогаются, миграция не нужна. Проверить
  надо и карточки заклинаний: те, что называют проверку словами, несут старое имя. **Файлы:**
  `src/ui/entities/character/lib/labels.ts`, `docs/glossary.md`,
  `src/core/infrastructure/catalog/thorne/spells/`. **Проверка:** прогон владения фразами плюс
  поиск старых имён по контенту.
- **З-97 ✅ сделано · Владения навыками Торна: шесть, а не четыре.** Добавляются «Ловкость рук» и «Выживание»;
  Аркана, Анализ, Природа и Внимательность уже стоят. Числа листа сойдутся сами: +5 и +4 — это
  модификатор плюс бонус мастерства. **Файлы:**
  `src/core/infrastructure/catalog/thorne/character.ts`. **Проверка:** unit «владения навыками Торна
  совпадают с листом».
- **З-98 ✅ сделано · Размер — средний.** В данных стоит крупный. **Файлы:** тот же файл персонажа.

---

## Порядок и параллельность

Состояние на 2026-08-16.

| Свободно сейчас | Ждёт другой волны | Ждёт данных |
|---|---|---|
| З-25…З-27 (поиск и отбор); волна 5 (навигация); З-80 (пул подсказки); этап 2 волны 6 (З-63…З-65) | волна 4 после волны 5; волна 7 после волны 5; экран «Ремесло» (З-62) ждёт «Ещё» из волны 5 | Этапы 3–4 волны 6 ждут ответов мастера; `OQ-51` ждёт ответа до З-62 |

Волна 4 идёт после волн 1 и 5: иерархия правится один раз, а не трижды. Волна 6 — самая крупная: её
этап 1 не задевает файлов остальных волн и идёт параллельно с ними.

**Как разводить параллельные задачи по файлам.** Пересечение разводится изоляцией в worktree, а не
очередью, но два места за первый прогон работ оказались горячими и стоили ручных слияний: цепочка
приведения сохранений (`src/core/domain/assembly/migration.ts` — три задачи подряд дописывали в неё
свой шаг) и форма договора (`src/contract/views.ts` — две задачи расширяли один вид с разных сторон).
Если две задачи в одном заходе трогают одно из этих мест, лучше отдать их одному исполнителю подряд.

**Чем закрывается работа целиком:** первая строка боевого списка видна на 320×568 при активной
концентрации, и до любой строки можно дотянуться поиском; ни один отказ не приходит по-английски и
не приходит далеко от места нажатия; три компонента названы каждый своим требованием, материал лежит
вещью и знает, для чего он нужен; отыгрыш не велит говорить там, где заклинание творится молча;
короткий отдых длится десять минут и делает ровно то, что делает десять минут.
