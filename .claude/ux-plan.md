# План работ: интерфейс, навигация, ремесло

Рабочий план для исполнителей, а не документ спеки. Он называет файлы, и потому живёт в `.claude/`, а
не в `docs/`: путь `src/…` в документе домена запрещён.

Здесь стоит только невыполненное. Что уже сделано, держат `git log` и сама спека: требование со
статусом и названным прогоном — и есть доказательство, что поведение существует. Сделанная карточка
отсюда удаляется целиком, и рассказа о закрытых волнах здесь не бывает.

**`npm run verify:full` на `main` зелёный:** 129 файлов прогонов, 2057 прогонов, 22 прогона браузера,
партия через бэкенд, покрытие логики 100 %. Прогон axe обходит все шесть режимов в обеих темах.

---

## Брифинг исполнителя

**Один агент — одна карточка — один коммит.** Карточка ниже описана так, чтобы начать без
доуточнений, и у каждой один и тот же состав: что делать и почему, затем строки **Спека**, **Файлы**,
**Проверка**. Карточка без этих трёх строк не спланирована.

### Слова: одна идея — одно слово

Правило `CLAUDE.md` действует и здесь. Синонимы разводят исполнителей быстрее, чем что-либо ещё,
поэтому спорные слова закреплены:

| Слово | Что означает | Чем не заменяется |
|---|---|---|
| Прогон | Любая автоматическая проверка: unit, компонентный, интеграционный | «тест», «сценарий» |
| Прогон браузера | Прогон, поднимающий сборку в Playwright | «E2E», «сценарий браузера» |
| Карточка | Описание одной задачи в этом файле | «таск», «пункт» |
| Раздающий | Тот, кто выдаёт карточки, сливает ветки и гоняет `verify:full` | «сливающий», «тот, кто сливает» |
| Рамка | Линия по контуру элемента | «обводка», «бордюр» |
| Подложка | Заливка под содержимым; её уровень — **ступень** | «поверхность» (занята алхимией: «Скользкая поверхность») |
| Спека | Документы в `docs/` | «документация», «требования» как собирательное |

### Что прочитать до первой строки кода

`CLAUDE.md` целиком, `docs/domains/README.md`, свою карточку и тот документ спеки, который она
правит. Экранная карточка добавляет к этому `docs/ux.md`. Больше ничего читать не нужно: если
карточка требует знания соседнего домена, это в ней названо.

### Карточка может врать адресом

Диагнозы аудита подтверждались все, а вот файлы за сутки разъезжались: причина погашенной кнопки жила
не в том каталоге, названный комментарий-оговорка уже исчез, числа контраста были сняты со скриншота и
при замере разошлись вдвое. Однажды ошибочное число задало карточке форму: условие приёмки не
выполнялось ещё до правки, и вся волна пошла иначе. Адрес и число из карточки — подсказка, а не факт:
**замеряй до того, как поверишь**, а расхождение называй в ответе.

### Порядок внутри карточки

Тот же, что в `CLAUDE.md`, и отступать от него нельзя:

1. **Сначала спека.** Новое поведение появляется требованием в документе своего домена, потом кодом.
   Правка без строки в `docs/` — неполный коммит.
2. **Реализация вливается сразу.** Заработало — в `main`. Держать готовое невлитым до конца волны
   запрещено.
3. **Проверка названа.** `Готово` и `Проверено` без имени прогона в строке «Проверка» не бывает. Имя
   пишется в обратных кавычках и обязано существовать в коде — за этим следит `npm run check:docs`.
4. **Прогон, ловящий дефект, пишется до правки.** Написанный после, он доказывает только то, что код
   делает то, что делает. Убедись, что он падает на старом коде, и скажи об этом в ответе.
5. **Перед правкой экрана — навык `frontend-design`, после правки — скриншот 390×844 в тёмной теме.**
   Скрытие информации игрок трактует против: спрятал факт — обоснуй в сообщении коммита.
6. **Механику не выдумывать.** Число, КС, длительность, стоимость берутся из подтверждённого
   источника; при сомнении — запись в `docs/open-questions.md`, а не догадка в коде.
7. **Параллельное — в отдельных worktree** под `.claude/worktrees/`, слияние из каталога `main`.

### Правило игрока, старше прочих

Записано в `docs/ux.md`, раздел «Общие правила», и им судится каждая экранная карточка:

> **Место и понятность — одно решение, а не два.** Факт вписывается в то, что уже стоит на экране, а
> не приписывается рядом второй строкой: дубль отнимает место и заставляет читать дважды одно и то
> же. Выбор «спрятать или дать вырасти» — признак, что решение не найдено: ищется третье, где факт
> назван целиком и не занял ни одной лишней строки.

Третье решение каждый раз находилось в одном и том же месте: **место занимал дубль либо то, чего в
этот момент не спрашивают.** Отказ повторял счёт, стоявший рядом; пять плашек вещи повторяли одно и то
же «+1»; имя шторки повторяло её заголовок; поле поиска встало на место переключателей, потому что
набирающий имя не спрашивает, какие тут реакции. Убрали лишнее — место нашлось. Приносить игроку
выбор между «спрятать» и «дать вырасти» — значит не доделать карточку.

### Что уже стоит в коде и роняет прогон, если это нарушить

Это не история, а действующие ограничения: их стережёт `npm run verify`.

- **Рамка в приложении одна и называет роль заклинания.** Класс `border` вне
  `src/ui/entities/spell/ui/SpellCardCompact.tsx` роняет `check:rules`. Линия края (`border-t`,
  `border-b`) рамкой не считается и разрешена.
- **Ступень подложки приходит от владельца.** Классы `bg-slate-*` и `bg-white` вне
  `src/ui/shared/ui/surface.ts` роняют `check:rules`. Ступеней три — страница, группа,
  нажимаемое, — а имён четыре: `SURFACE_PAGE`, `SURFACE_GROUP`, `SURFACE_CONTROL` и
  `SURFACE_PANEL`, который берёт ступень группы и добавляет тень.
- **Подпись на подложке своего же цвета берёт светлый вариант** `text-<тон>-bright`: тон и подложка
  из него же не могут быть одной светлоты. Иначе падает прогон axe по тёмной теме.
- **Фраза живёт в одном файле.** Русский литерал длиннее пятнадцати знаков во втором рабочем файле
  роняет `check:rules`.

### Ограничения прогона, которые стоили времени

- **Прогоны браузера в worktree не запускать.** `playwright.config.ts` держит порты 4173/4174 с
  `reuseExistingServer: true` локально: два параллельных worktree дадут зелёный прогон против чужой
  сборки. Исполнитель гоняет `npm run verify`; `npm run verify:full` идёт на `main` после слияния, и
  делает это раздающий. Сами прогоны браузера при этом **править обязательно**, если карточка меняет
  подписи, — иначе они упадут у раздающего.
- **Офлайн-прогон чувствителен к нагрузке.** `serves the app from cache when the network is gone`
  ждёт service worker десять секунд; на загруженной машине это падает, на свободной проходит за
  0.6 с. Сам worker исправен. Падение при живых параллельных карточках — не повод чинить приложение;
  перепроверь на тишине.
- **После ребейза гнать полный прогон, а не `check:docs`.** Две ветки меняли форму одного поля
  договора с разных сторон, git свёл их без конфликта, и разъехались они на типах: чистое слияние
  само по себе ничего не доказывает.
- **`node_modules` копировать только в свежий worktree** (`cp -Rc` с APFS — мгновенно). Копия в
  каталог, где они уже есть, вкладывает их внутрь себя, даёт две копии React и 224 «упавших»
  прогона, к коду отношения не имеющих.
- **Prettier в репозитории не настроен.** Прогон `npx prettier --write` переформатирует файл под 80
  колонок против принятых ста и превращает правку в семь строк из двухсот. Форматировать руками.

### Номера идентификаторов раздаются заранее

Параллельные карточки берут «следующий свободный» одновременно и друг друга не видят, и каждый развод
стоит коммита. **Раздающий выдаёт каждой непересекающийся диапазон** `FR`, `OQ` и `ADR` прямо в
задании.

Занято: `FR` — по 316, из трёхсотых свободны `FR-307`…`FR-309`, `FR-312`…`FR-315` и всё от `FR-317`;
`OQ` — по 50; `ADR` — по 0061.

Столкновение номера — не единственная беда: трижды подряд слияние вставало на том, что два
исполнителя дописывали свою запись в конец одного документа (`docs/agreements.md`, `docs/screens.md`
дважды). Разводится это руками за минуту и потому дешевле очереди, но раздающий должен закладывать
эти минуты в каждое слияние.

### Новый режим экрана заводится в двух местах

Правило общее для всех карточек, которые заводят режим, и потому стоит здесь, а не в каждой из них:
ярлык добавляется в список `UNDER_MORE` (`src/ui/features/screen-mode/ui/BottomNav.tsx`) и в
`SCREEN_MODES` (`src/ui/shared/model/screenMode.ts`). Панель при этом не растёт: под «Ещё» помещается
список любой длины.

### Шаблон задания

Агенту (`implementer` или общего назначения) даётся ровно это, с подставленным номером:

```
Ты работаешь в git worktree <путь>, ветка agent/<имя>. Работай только там. Ветку не
переключай, в main не сливай — слияние делает раздающий. node_modules уже стоят.

Карточка З-NN из /абсолютный/путь/.claude/ux-plan.md (файл лежит вне worktree, читай по
абсолютному пути). Прочитай CLAUDE.md, docs/domains/README.md, брифинг исполнителя в том
же файле, свою карточку целиком и тот документ спеки, который она правит; экранная
карточка добавляет docs/ux.md. Сделай ровно её и ничего сверх.

Порядок: строка требования в docs/ → код → прогон с именем из строки «Проверка».
Один коммит на законченную мысль, сообщение по-английски.

Экран трогаешь — сначала навык frontend-design, после правки скриншот 390×844 в тёмной
теме своим скриптом на выделенном тебе порту <N>. MCP-браузером не пользуйся: он один
на все параллельные карточки.

Прогон: npm run verify. Прогоны браузера не запускай — Playwright держит порты 4173/4174
с reuseExistingServer, параллельные карточки дадут зелёный прогон против чужой сборки;
verify:full идёт на main после слияния. Но сами прогоны браузера, спрашивающие
изменённые подписи, поправь. Красный npm run verify — карточка не сдана.

Свободный диапазон номеров: FR-<a>…FR-<b>, OQ-<c>, ADR-<d>. Их точно никто не займёт.

Параллельно идут: <перечень карточек и их файлов>. В их файлы не лезь.

Остановись и верни вопрос, не делая правки, если: понадобилось игровое число, которого
нет в подтверждённом источнике; правка отменяет принятое ADR; понадобилось новое ребро
между контекстами.
```

**Что просить в ответе:** что изменил, номер требования, имя прогона, хеш коммита, путь к снимку,
итог `npm run verify`, и — отдельно — что сделал сверх карточки и почему, что отказался делать и
почему. Оба этих пункта в ответах оказались самыми ценными.

### Когда карточка сделана

Прогон зелёный; требование в `docs/` стоит со статусом и названным прогоном; прогон с этим именем
существует; для экранных карточек приложен скриншот; коммит в `main` один и читается как законченная
мысль; **карточка удалена из этого файла**.

### Как разводить параллельные карточки по файлам

Пересечение разводится изоляцией в worktree, а не очередью, но два места оказались горячими и стоили
ручных слияний: цепочка приведения сохранений (`src/core/domain/assembly/migration.ts`) и форма
договора (`src/contract/views.ts`). Если две карточки в одном заходе трогают одно из этих мест, лучше
отдать их одному исполнителю подряд.

---

## Что свободно сейчас

Порядка между свободными карточками нет: файлы у них не пересекаются, и идут они одновременно.

| Свободно | Ждёт ответа мастера |
|---|---|
| З-26, З-27 (отбор); З-80 (пул подсказки); З-62…З-65 (ремесло: экран и калькулятор); З-90 (заметки) | З-66…З-68 (ремесло: изготовление и исследование) — `OQ-49`, `OQ-50` |

---

## Отбор — З-26, З-27

Поиск по названию сделан (FR-303): до любой строки можно дотянуться, не прокручивая пятнадцать.
Осталось то, что отвечает на другой вопрос — не «где эта строка», а «что тут вообще есть».

**Жёсткое условие обеих карточек: ни одного лишнего пикселя над списком.** Полоса фильтров
переносится на второй ряд, а не прокручивается, поэтому лишний переключатель стоит целого ряда — и
съедает ровно тот ряд, который освободил поиск.

### З-26 · Раздел «Часто»

Три–пять недавно применённых строк наверху списка, собираются из журнала: в бою повторяют то, что уже
творили.

**Спека:** новый `FR` в `docs/screens.md`. **Файлы:** `src/core/presentation/views/spellRowsView.ts`,
`src/ui/screens/game/`. **Проверка:** unit `часто применённое собирается из журнала`.

### З-27 · Роль отбирает и в «Игре»

FR-212 держит роль только в «Книге» с обоснованием «вопрос неспешного разбора». Первая половина
обоснования спорна: FR-211 сам говорит, что роль отвечает на вопрос «зачем это нужно», а в бою это и
есть вопрос — ударить или защититься. Вторая половина в силе, поэтому роль приходит **вместо**
переключателя времени накладывания, а не в дополнение.

**Спека:** правит FR-002 и FR-212 в `docs/screens.md`. **Файлы:**
`src/ui/features/filter-spells/`. **Проверка:** компонентный
`полоса фильтров «Игры» остаётся одним рядом на 320`.

---

## З-80 · «Последняя подсказка» пулом

Активная способность предыстории «Нерадивый ученик»: один раз до долгого отдыха, при провале проверки
Интеллекта, связанной с рунами, надписью, шифром, ритуалом, головоломкой или магическим механизмом,
бросок повторяется, к новому результату прибавляется бонус мастерства, новый результат обязателен.
Восстанавливается долгим отдыхом — ровно как руны и бюджет магического восстановления, и живёт тем же
пулом.

Пул 1/1 рядом с рунами и очками: тем же способом хранится, тем же способом тратится, той же операцией
возвращается. Показывается в ряду прочих ресурсов. Ноль — состояние: истраченная подсказка остаётся
строкой с нулём.

**Границу применения задал игрок: на алхимию способность не распространяется**, и в мастере
изготовления её не предлагают — подсказка про руны и загадки, а не про реторты.

**Спека:** новый `FR` в `docs/domains/arcana.md`. **Файлы:** `src/core/domain/arcana/schema.ts`,
`src/core/domain/arcana/arcana.ts`, `src/core/application/useCases/rest.ts`,
`src/ui/widgets/resource-header/`. **Проверка:** интеграционный
`подсказка тратится один раз и возвращается долгим отдыхом`.

---

## Ремесло — З-62…З-68

Домен и контекст `crafting` заведены, ребро `crafting → catalog` вписано в `check:layers`, словарь
свойств и ингредиент как знание — в `main` (FR-300, FR-301). Вещь про ремесло не знает: «куда годится
этот корень» выводится обходом рецептов — тем же механизмом, каким строка вещи называет, чем она
требуется.

**Источник — «Алхимия Сумрачного Доминиона. Справочник игрока», редакция 31 июля 2026**, домашняя
система стола, не SRD. Файл лежит в корне рабочего каталога и **в git не закоммичен**: у продолжающего
на чистом клоне его не будет, и числа брать будет неоткуда — попроси файл, а не выводи по образцу.

Границу возможного задал сам справочник: он прямо говорит, что не содержит ни описаний отдельных
ингредиентов, ни механики конкретных эффектов. Приложение считает **сложность, время, расходники и
партию** — это полные формулы — и **не считает результат состава**: эффект называется словом, его
числа остаются столу (ADR-0056).

### Числа Торна, проверяемые по листу

Интеллект 18 даёт модификатор +4, бонус мастерства на 7 уровне равен +3. Владеет **зельеварением** и
**трансмутацией**; **синтезом ядов не владеет** и изучать не собирается.

| Направление | Проверка Торна |
|---|---|
| Зельеварение | 1к20 + 3 + 4 = **1к20 + 7** |
| Трансмутация | 1к20 + 3 + 4 = **1к20 + 7** |
| Синтез ядов (не изучен) | 1к20 + 4 |
| Гибрид с участием ядов | 1к20 + 4 — справочник берёт **наименьший** бонус среди задействованных направлений |

Последнее — не мелочь, и экран обязан это называть: рецепт, куда затесалось ядовитое свойство, роняет
проверку на три. Игрок должен видеть причину до броска, а не после провала.

Оснащение — **надёжные походные комплекты** по обоим изученным направлениям, 180 зм за направление.
Отсюда пределы, и они жёстче, чем кажется:

| Что задаёт оснащение | Значение у Торна |
|---|---|
| Предельная сложность | **20** — рецепт сложнее невозможен, а не «сложен» |
| Предел партии | **6** рецептурных порций → 6 + ⌊6/4⌋ = **7** единиц состава |
| Доступное исследование | только **первое и второе** свойства; третье и четвёртое требуют стационарной лаборатории |

Это и делает калькулятор нужным: база 10 плюс редкий эффект (+5) плюс усиление тремя источниками (+3)
плюс минута длительности (+2) уже даёт 20 — предел. Отказ «Сложность 22 выше предела набора: 20»
будет звучать часто, и он обязан называть, **чем именно** набрано лишнее, а не просто гаснуть.

### З-62 · Экран «Ремесло»

Новый режим под «Ещё». Ингредиент остаётся вещью в «Вещах» со своим счётом порций; в «Ремесле» живёт
знание. На строке вещи — производная подпись «раскрыто 2 · следующее не исследовано»: знаменателя
приложение не обещает, пока игрок не отметил, что свойств больше нет. С отметкой подпись становится
«раскрыто 2 из 2».

**Спека:** FR-306 в `docs/domains/crafting.md` переводится из `План` в `Готово`, новый `FR` на сам
режим — в `docs/screens.md`. **Файлы:** новый экран в `src/ui/screens/`, `BottomNav.tsx`,
`screenMode.ts`. **Проверка:** компонентный `«Ремесло» показывает раскрытое знание, а не запас`.

### З-63 · Совпадения

Выбраны 2–4 вида — приложение называет, какие свойства совпали (свойство входит при двух и более
разных источниках), сколько у каждого источников и какая отсюда ступень: два источника — обычная, три
— усиленная (+3 Сл, около ×1,5), четыре — концентрированная (+6 Сл, около ×2). Несколько порций
одного вида совпадения не создают. **Главное правило безопасности из справочника — на экране:** пока
состав не очищен, действуют **все** совпавшие свойства, а не только желаемое.

**Спека:** новый `FR` в `docs/domains/crafting.md`. **Файлы:**
`src/core/domain/crafting/crafting.ts`, `src/core/domain/crafting/schema.ts`. **Проверка:** unit
`совпавшим считается свойство от двух и более разных источников`.

### З-64 · Расчёт сложности

База 10, итог не ниже 5. Восемь групп модификаторов: редкость основного и дополнительных эффектов,
ступень усиления, длительность, начало действия, периодичность, цели и область, способ применения,
сопротивление; затем очистка (+5) или подавление по редкости; затем ограничения и последствия с
потолком −6. Всё считает ядро — экран не считает ничего.

**Спека:** новый `FR` в `docs/domains/crafting.md`. **Файлы:**
`src/core/domain/crafting/crafting.ts`. **Проверка:** unit
`сложность складывается из восьми групп и не падает ниже пяти`.

### З-65 · Итог и предел оснащения

По итоговой сложности: время партии, класс и цена расходников за начатый час, число готовых единиц
(порции + ⌊порции/4⌋). Оснащение бонуса не даёт — оно задаёт предел сложности и размер партии; работа
без профильного набора добавляет +5 и делит партию вдвое. Сложность выше предела — отказ словами, а не
погашенная кнопка.

**Спека:** новый `FR` в `docs/domains/crafting.md`. **Файлы:**
`src/core/domain/crafting/crafting.ts`, `src/core/application/useCases/`. **Проверка:** unit
`сложность выше предела набора отклоняется с причиной, называющей лишнее`.

### З-66 · Расход одной записью журнала

Списывается по одной порции каждого вида на каждую рецептурную порцию. `adjustBagCount` меняет одну
вещь за вызов и пишет свою запись — изготовление из трёх ингредиентов дало бы три отмены на одну
операцию и вернуло бы половину рецепта.

**Спека:** новый `FR` в `docs/domains/crafting.md`. **Файлы:** `src/core/application/useCases/`.
**Проверка:** интеграционный `изготовление списывает все виды одной записью журнала`.

### З-67 · Проверка разработки и известный рецепт

Бросок делает игрок, приложение принимает результат и сравнивает со сложностью (ADR-0021): 1к20 +
профильный навык, если изучен, + модификатор Интеллекта. Натуральные 20 и 1 названы своими
последствиями, критический провал — таблицей d6. После первого успеха рецепт записан и повторяется без
броска, пока совпадают все четыре условия справочника; замена даже одного вида ингредиента — новая
формула и новая проверка.

**Спека:** новый `FR` в `docs/domains/crafting.md`. **Файлы:**
`src/core/domain/crafting/crafting.ts`, `src/core/application/useCases/`. **Проверка:**
интеграционный `известный рецепт повторяется без броска, пока совпадают все четыре условия`.

### З-68 · Раскрытие по порядку

Сырая проба и поверхностный анализ для первого свойства; таблица «свойство → время, Сл, оснащение,
расход порций» для второго-четвёртого, плюс модификатор редкости и расходники по классу за каждый
начатый час. Третье и четвёртое требуют стационарной лаборатории.

**Спека:** новый `FR` в `docs/domains/crafting.md`. **Файлы:**
`src/core/domain/crafting/crafting.ts`. **Проверка:** unit
`третье свойство требует стационарной лаборатории`.

---

## З-90 · Заметки про мир

**Решение игрока: текст и время, больше ничего.** Свободная запись про мир: одно поле, отправка по
«Ввод», свежее сверху, время в строке формата «ЧЧ:ММ» — тем же способом, каким журнал показывает
событие. Ни меток, ни разделов, ни привязки к вещам: вводом в чужой ход платить нельзя. Запись
правится и удаляется из своей строки. Поиск по слову приходит тем же механизмом, что и поиск по
заклинаниям (FR-303). Заметка **на предмете** — отдельная вещь: у вещи поле заметки уже есть и
остаётся как есть.

Новый режим под «Ещё». Где записи хранятся — решает `ADR`: журнал существует ради обратимости, и
складывать в него то, что не отменяется, значит смешать две сущности.

**Спека:** новый `FR` в `docs/screens.md`, запись в `docs/glossary.md` (заметка про мир против
заметки на вещи — два разных слова) и запись `ADR` о месте хранения. **Файлы:** новый экран в
`src/ui/screens/`, `BottomNav.tsx`, `screenMode.ts`, новый контекст в `src/core/domain/`.
**Проверка:** компонентный `запись заводится одним полем и переживает перезапуск`.

---

## Открыто и ждёт решения

Ничего из этого не блокирует работу: везде выбрано откатываемое поведение. У каждой записи назван
способ её закрыть — взгляд игрока за столом либо запись `ADR`. Неизвестные **правила игры** сюда не
пишутся: их место `docs/open-questions.md`.

- **Тролль на нуле хитов больше не встаёт сам концом боя.** Инвариант жизнеспособности так и велел,
  но код его прежде не соблюдал, и за столом играли иначе. Закрывается взглядом игрока.
- **Плитки шапки обещают «Правку»** там, где кнопка подтверждает: «Хиты 60/60. Правка: урон,
  лечение, временные». По FR-264 это спорно, но доступное имя плитки говорит, что откроется, а не
  как зовётся шторка. Закрывается взглядом игрока: сочтёт нужным — отдельная карточка с отдельным
  требованием.
- **Редкость свойства хранится у раскрытой ячейки**, и потому два ингредиента могут разойтись в
  редкости одного и того же свойства — никто их не остановит. Справочник редкости не печатает, и
  свести их было бы догадкой. Закрывается взглядом игрока.
- **Имя поля в русской фразе — латинский идентификатор схемы**: «поле „hitPoints.maximumBase“».
  Закреплено прогоном FR-121, поэтому меняется только вместе с требованием: своего словаря имён полей
  у приложения нет. Закрывается взглядом игрока.
- **Разбор на границе договора говорит по-английски и иначе не может**: договор не импортирует из
  `src/` ничего, а русские причины живут в основе домена. Видно это только сетевой поставке, где
  стороны могут разъехаться версиями. Закрывается записью `ADR`: либо словарь причин заводится внутри
  договора, либо записывается согласие, что нарушение договора игроку словами не объясняют.

---

**Чем закрывается работа целиком:** в боевом списке видно не только «где эта строка», но и «что тут
вообще есть»; подсказка предыстории живёт тем же пулом, что руны; ремесло считает сложность, время,
расходники и партию и честно молчит о том, чего в справочнике нет; заметка про мир заводится одним
полем.
